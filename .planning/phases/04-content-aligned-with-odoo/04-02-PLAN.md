---
phase: 04-content-aligned-with-odoo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - www/src/app/[locale]/features/page.tsx
  - www/messages/en/pages.json
  - www/messages/bg/pages.json
  - www/src/app/[locale]/pricing/page.tsx
  - www/messages/en/pricingPreview.json
  - www/messages/bg/pricingPreview.json
autonomous: true
requirements:
  - CONTENT-02
  - CONTENT-03
must_haves:
  truths:
    - "Features page shows Employee Management, Calculators, Compliance as active features with full detail"
    - "Features page shows ATS, Leave, Payroll, Performance, Onboarding in a Roadmap section with Coming Soon badges"
    - "Pricing page shows a single HR Platform plan with employee management included"
    - "Pricing page shows unbuilt modules as future add-ons with Coming Soon labels"
    - "Feature comparison table only shows available features"
  artifacts:
    - path: "www/src/app/[locale]/features/page.tsx"
      provides: "Restructured features page with active + roadmap sections"
      contains: "ACTIVE_FEATURES"
    - path: "www/src/app/[locale]/pricing/page.tsx"
      provides: "Single plan pricing with coming soon modules"
      contains: "Coming soon"
    - path: "www/messages/en/pages.json"
      provides: "Rewritten features and pricing translation content"
  key_links:
    - from: "www/src/app/[locale]/features/page.tsx"
      to: "www/messages/en/pages.json"
      via: "t('sections.${key}.title') translation lookups"
      pattern: "sections\\.(employees|calculators|compliance)"
    - from: "www/src/app/[locale]/pricing/page.tsx"
      to: "www/messages/en/pages.json"
      via: "t('modules.${key}.name') translation lookups"
      pattern: "modules\\.coreHr"
---

<objective>
Restructure features page to show real capabilities prominently and unbuilt modules in a roadmap section. Restructure pricing page from 6-module model to single plan with coming-soon add-ons.

Purpose: Features and pricing pages are the two key conversion pages. They must accurately represent what the product does today vs what is planned.
Output: Features page with active/roadmap split, pricing page with single plan + future modules.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-aligned-with-odoo/04-CONTEXT.md
@.planning/phases/04-content-aligned-with-odoo/04-RESEARCH.md

@www/src/app/[locale]/features/page.tsx
@www/src/app/[locale]/pricing/page.tsx
@www/messages/en/pages.json
@www/messages/en/pricingPreview.json
@www/src/components/sections/feature-row.tsx

<interfaces>
<!-- Features page reads translations via t('sections.${key}.title'), t('sections.${key}.description'), t.raw('sections.${key}.features') -->
<!-- Pricing page reads via t('modules.${key}.name'), t('modules.${key}.description'), t.raw('modules.${key}.features') -->
<!-- Pricing comparison: FEATURE_ROWS array indexes into FEATURE_MATRIX boolean arrays, one boolean per MODULE_KEYS entry -->
<!-- FeatureRow component props: { id, title, description, features: string[], imagePlaceholder, reversed } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure features page with active features and roadmap section</name>
  <files>
    www/src/app/[locale]/features/page.tsx,
    www/messages/en/pages.json,
    www/messages/bg/pages.json
  </files>
  <action>
    1. Edit `www/src/app/[locale]/features/page.tsx`:
       - Replace FEATURE_SECTIONS with two arrays:
         ```
         const ACTIVE_FEATURES = [
           { id: 'employees', key: 'employees', reversed: false },
           { id: 'calculators', key: 'calculators', reversed: true },
           { id: 'compliance', key: 'compliance', reversed: false },
         ] as const
         const ROADMAP_FEATURES = [
           { id: 'ats', key: 'ats' },
           { id: 'leave', key: 'leave' },
           { id: 'payroll', key: 'payroll' },
           { id: 'performance', key: 'performance' },
           { id: 'onboarding', key: 'onboarding' },
         ] as const
         ```
       - Render ACTIVE_FEATURES using existing FeatureRow loop (keep alternating backgrounds)
       - After active features, add a "Roadmap" section:
         - Section heading from t('roadmap.heading') and t('roadmap.subheading')
         - Render ROADMAP_FEATURES as a compact grid (2-3 cols) of cards, each with:
           - Title from t(`sections.${key}.title`)
           - Brief description from t(`sections.${key}.description`) (1-2 sentences)
           - "Coming soon" amber badge: `<span className="inline-flex items-center rounded-full bg-amber-100 px-2.5 py-0.5 text-xs font-medium text-amber-800">{t('roadmap.comingSoon')}</span>`
       - Keep CTA at bottom

    2. Edit `www/messages/en/pages.json` features section:
       - Rewrite hero.heading: "HR Platform Features" (no "AI-Powered")
       - Rewrite hero.subheading: Honest description of available features + coming roadmap
       - Rewrite metaTitle/metaDescription: Remove AI claims, focus on employee management + Bulgarian compliance
       - Rewrite sections.employees: Keep title, rewrite description to match real capabilities (employee directory with search/filter, CRUD, dashboard). Update features array to real feature list (employee profiles, directory search, add/edit employees, department filtering, bilingual interface)
       - Add sections.calculators: title "HR Calculators & Tools", description about salary calculator and freelancer comparison. Features: salary calculator with 2026 tax rates, EOOD vs employment comparison, net/gross calculations, social security breakdown, visual savings highlights
       - Add sections.compliance: title "Bulgarian Compliance", description about built-in BG labor law compliance. Features: 2026 tax rates built-in, social security calculations, labor code awareness, Bulgarian-first interface, NRA-compatible calculations
       - Keep sections.ats, .leave, .payroll, .performance, .onboarding but shorten descriptions to 1-2 sentences each (these appear in roadmap cards, not full feature rows)
       - Add roadmap object: `"roadmap": { "heading": "Coming Soon", "subheading": "We are building these modules next. Stay tuned.", "comingSoon": "Coming soon" }`

    3. Create matching BG translations in `www/messages/bg/pages.json` for ALL changed keys. The BG pages.json is large -- only update the features section keys that changed. Do NOT touch other sections (hrTools, etc.) unless they contain fabricated claims.

    CRITICAL: The features array for each section must be a JSON array of strings. The FeatureRow component reads it via `t.raw('sections.${key}.features') as string[]`.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr && make build-www</automated>
  </verify>
  <done>Features page shows 3 active feature rows (Employee Management, Calculators, Compliance) with full detail, followed by a Roadmap section with 5 compact cards (ATS, Leave, Payroll, Performance, Onboarding) each bearing "Coming soon" badges. EN and BG translations match.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure pricing page to single plan with coming-soon add-ons</name>
  <files>
    www/src/app/[locale]/pricing/page.tsx,
    www/messages/en/pages.json,
    www/messages/bg/pages.json,
    www/messages/en/pricingPreview.json,
    www/messages/bg/pricingPreview.json
  </files>
  <action>
    1. Edit `www/src/app/[locale]/pricing/page.tsx`:
       - Split MODULE_KEYS into two:
         ```
         const ACTIVE_MODULES = ['coreHr'] as const
         const COMING_SOON_MODULES = ['ats', 'leave', 'payroll', 'performance', 'onboarding'] as const
         ```
       - Module Cards section: Render ACTIVE_MODULES as full cards (current styling), then render COMING_SOON_MODULES as muted cards with reduced opacity (opacity-60) and a "Coming soon" amber badge in the top-right corner. Remove the "Get a quote" button from coming-soon cards; replace with the badge.
       - Feature Comparison Table: Simplify to show only the coreHr column as active. Either:
         (a) Show only coreHr features in a simple checklist (no multi-column table), OR
         (b) Keep multi-column table but add "Coming soon" labels in the header for non-coreHr columns and gray out those columns
         Choose option (a) for simplicity -- a single-column feature list for what's available now.
       - Update FEATURE_ROWS to only include coreHr features: employeeProfiles, orgChart, documentStorage
       - Update FEATURE_MATRIX to match (single boolean column for coreHr)
       - Keep Quote Form and FAQ sections

    2. Edit pricing section of `www/messages/en/pages.json`:
       - metaTitle: "Pricing | HR" (remove AI claims)
       - metaDescription: Honest description about HR platform pricing
       - heading: "Simple pricing for growing teams" (not "built for ROI")
       - subheading: "Start with employee management. Add modules as we build them."
       - modulesHeading: "Available now"
       - modulesSubheading: "Start managing your employees today."
       - Keep modules.coreHr content as-is (it's accurate)
       - Keep modules for ats/leave/payroll/performance/onboarding but add a note these are coming soon (the badge handles visual indication, descriptions stay for context)
       - Add `"comingSoon": "Coming soon"` key
       - Update comparison section: comparisonHeading to "What's included", comparisonSubheading to "Features available in the HR Platform today."
       - Keep only coreHr-related comparison rows
       - Update FAQ answers: question about modules should mention current + planned. Remove claims about "per-module pricing" for modules that don't exist yet.

    3. Rewrite `www/messages/en/pricingPreview.json` (homepage pricing preview):
       - heading: "Simple pricing" (not "Modular pricing")
       - subheading: "Start with employee management. More modules coming soon."
       - seeAllModules: "See pricing details" (not "See all 6 modules")
       - modules: Keep coreHr, mark ats and leave as coming soon in their descriptions
       - Or simplify to show only coreHr module in preview

    4. Create matching BG translations for all changed keys in pages.json and pricingPreview.json.

    IMPORTANT: When changing FEATURE_ROWS and FEATURE_MATRIX, ensure array lengths match. Each FEATURE_MATRIX entry must have exactly as many booleans as there are entries in the active modules array (just 1 for coreHr). If using single-column approach, each feature just needs `[true]`.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr && make build-www</automated>
  </verify>
  <done>Pricing page shows coreHr as the active plan with full card + "Get a quote" CTA. Five coming-soon modules shown as muted cards with amber badges. Feature comparison shows only available features. FAQ updated. Homepage pricing preview updated. EN and BG translations match.</done>
</task>

</tasks>

<verification>
- `make build-www` succeeds
- Features page at /en/features shows 3 active features + roadmap section
- Pricing page at /en/pricing shows single active plan + coming soon modules
- No claims about unbuilt features being available
- All translation keys resolve in both EN and BG
</verification>

<success_criteria>
- Features page accurately represents real vs planned capabilities
- Pricing page shows single plan model (not 6-module)
- Coming Soon badges visible on all unbuilt modules
- Build passes, no missing translation warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-aligned-with-odoo/04-02-SUMMARY.md`
</output>

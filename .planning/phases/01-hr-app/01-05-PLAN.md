---
phase: 01-hr-app
plan: 05
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/hr/src/app/[locale]/pricing/page.tsx
  - app/hr/src/components/ui/quote-form.tsx
  - app/hr/messages/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Pricing page shows per-module structure with no public prices"
    - "Each module has a 'Get a quote' CTA (not a price)"
    - "Feature comparison table shows checkmarks for what's included in each module"
    - "No actual currency amounts appear on the pricing page"
  artifacts:
    - path: "app/hr/src/app/[locale]/pricing/page.tsx"
      provides: "Pricing page with per-module quote model"
      contains: "quote"
    - path: "app/hr/src/components/ui/quote-form.tsx"
      provides: "Get a quote form modal component"
      contains: "use client"
  key_links:
    - from: "app/hr/src/app/[locale]/pricing/page.tsx"
      to: "QuoteForm"
      via: "import from @/components/ui/quote-form"
      pattern: "QuoteForm"
---

<objective>
Redesign the pricing page from tiered plans with prices to a per-module "Get a quote" model with feature comparison table.

Purpose: Per user decision, pricing should NOT show public prices. Instead, each module has a "Get a quote" form. A feature comparison table shows what's included in each module.
Output: Pricing page with module cards (no prices), feature comparison table, and quote request form.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hr-app/15-CONTEXT.md
@.planning/phases/15-hr-app/15-01-SUMMARY.md
@app/hr/src/app/[locale]/pricing/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuoteForm component and redesign pricing page</name>
  <files>app/hr/src/components/ui/quote-form.tsx, app/hr/src/app/[locale]/pricing/page.tsx, app/hr/messages/en.json</files>
  <action>
Create app/hr/src/components/ui/quote-form.tsx:
- 'use client' directive
- import { useState } from 'react'
- import { useForm } from 'react-hook-form'
- import { zodResolver } from '@hookform/resolvers/zod'
- import { z } from 'zod'
- Schema: { name: string, email: string().email(), company: string, employees: string (select: 1-10, 11-50, 51-200, 200+), modules: string[] (checkboxes), message: string().optional() }
- Props: { selectedModule?: string, labels: Record<string, string> }
- On submit: store in localStorage as lead capture (key: 'quote_requests', append to array)
- Show success message after submit
- Styled with corporate palette: bg-white rounded-2xl p-8 border border-gray-200 shadow-lg
- Form fields: name, email, company name, company size (select), interested modules (checkboxes), optional message
- Submit button: "Request a quote" / "Get a quote" — bg-primary text-white

Redesign pricing/page.tsx:
- COMPLETELY REMOVE the old tiered pricing cards with prices
- New layout:
  1. Hero section: bg-navy-deep text-white py-20, heading "Simple, transparent pricing", subheading "Choose the modules you need. Pay only for what you use."
  2. Module cards section: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
     - 6 module cards (Core HR, ATS, Leave Management, Payroll, Performance, Onboarding)
     - Each card: bg-white rounded-2xl p-8 border border-gray-200 shadow-sm hover:shadow-md transition
     - Icon (SVG), module name (h3), short description, bullet list of what's included
     - CTA: "Get a quote" button linking to the quote form section below
     - NO price, NO "per month", NO currency values
  3. Feature comparison table: full-width table
     - Rows: feature names (e.g., "Employee profiles", "Leave requests", "Payroll processing")
     - Columns: each module
     - Cells: checkmark (included) or dash (not included)
     - Styled: alternating row backgrounds, sticky header on mobile
  4. QuoteForm section at bottom with id="quote"
     - Heading: "Get a personalized quote"
     - The QuoteForm client component
  5. FAQ section below form (3-5 common pricing questions)
     - Use simple details/summary or a styled accordion

Update en.json pages.pricing section:
- Remove all price strings (plans.starter.price, plans.growth.price, etc.)
- Add modules object with 6 modules, each having: name, description, features (string[])
- Add comparison table data
- Add FAQ items
- metaTitle, metaDescription updated for "Get a quote" model
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && ! grep -q "\$\|BGN\|€\|лв\.\|/month\|/year\|/месец" src/app/\\[locale\\]/pricing/page.tsx && grep -q "quote\|Quote" src/app/\\[locale\\]/pricing/page.tsx && test -f src/components/ui/quote-form.tsx && echo "PASS"</automated>
    <manual>Verify no actual prices are visible on the pricing page</manual>
  </verify>
  <done>Pricing page shows 6 module cards without prices, feature comparison table with checkmarks, quote request form at bottom, FAQ section; zero currency values on the page</done>
</task>

</tasks>

<verification>
- No currency amounts ($, BGN, EUR, лв.) appear on the pricing page
- 6 module cards each have "Get a quote" CTA
- Feature comparison table renders with checkmarks
- QuoteForm collects name, email, company, size, modules, message
- Form submission stores to localStorage
- FAQ section present
</verification>

<success_criteria>
- `bun run build` succeeds
- grep confirms no price strings in pricing page
- QuoteForm renders and submits without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-hr-app/15-05-SUMMARY.md`
</output>

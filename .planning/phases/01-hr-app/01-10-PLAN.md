---
phase: 01-hr-app
plan: 10
type: execute
wave: 3
depends_on: ["15-02", "15-03", "15-04", "15-05", "15-06", "15-07", "15-08", "15-09"]
files_modified:
  - app/hr/next.config.mjs
  - app/hr/src/middleware.ts
  - app/hr/src/app/[locale]/blog/page.tsx
  - app/hr/messages/bg.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Blog proxy rewrites /blog/* to the Astro hr-blog app"
    - "next-intl middleware excludes /blog paths from locale injection"
    - "bg.json has Bulgarian translations for all new page content"
    - "App builds successfully with all pages and routes"
  artifacts:
    - path: "app/hr/next.config.mjs"
      provides: "Blog proxy rewrite rules"
      contains: "beforeFiles"
    - path: "app/hr/messages/bg.json"
      provides: "Complete Bulgarian translations"
      min_lines: 500
  key_links:
    - from: "app/hr/next.config.mjs"
      to: "http://localhost:3013"
      via: "rewrites beforeFiles"
      pattern: "3013"
---

<objective>
Set up blog integration via Next.js rewrite proxy and complete all Bulgarian translations.

Purpose: Blog proxy enables seamless /blog/* URLs served by the Astro hr-blog. Bulgarian translations complete the i18n coverage so the site works fully in both languages. This plan runs last because it depends on all content being finalized.
Output: Working blog proxy, complete bg.json translations, passing build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hr-app/15-CONTEXT.md
@.planning/phases/15-hr-app/15-RESEARCH.md
@app/hr/next.config.mjs
@app/hr/src/middleware.ts
@app/hr/messages/bg.json
@app/hr/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure blog proxy rewrite and update middleware</name>
  <files>app/hr/next.config.mjs, app/hr/src/middleware.ts, app/hr/src/app/[locale]/blog/page.tsx</files>
  <action>
Update next.config.mjs to add blog proxy rewrites:
- Add async rewrites() function returning beforeFiles rules:
  ```
  async rewrites() {
    return {
      beforeFiles: [
        {
          source: '/blog',
          destination: `${process.env.HR_BLOG_URL || 'http://localhost:3013'}/`,
        },
        {
          source: '/blog/:path*',
          destination: `${process.env.HR_BLOG_URL || 'http://localhost:3013'}/:path*`,
        },
      ],
    }
  }
  ```
- This must be added to the nextConfig object before the plugins are applied

Update middleware.ts:
- Ensure the next-intl middleware matcher EXCLUDES /blog paths
- The middleware should NOT intercept /blog/* requests (otherwise it redirects to /en/blog/* or /bg/blog/*)
- Update the matcher config or add a condition to skip blog paths
- Check current middleware implementation and adjust accordingly
- Common pattern: update config.matcher to exclude blog, OR add a condition at the top of the middleware function that returns NextResponse.next() for /blog paths

Update blog/page.tsx:
- Keep as a simple page that explains the blog is loading or provides a fallback
- This page only shows if the rewrite fails (Astro blog not running)
- Show: "Loading blog..." or redirect to /blog via the rewrite
- Alternatively, make this a catch-all [...slug]/page.tsx that acts as fallback

NOTE: The Astro hr-blog has its own header/footer. Per research recommendation, approach (a): style the Astro blog header/footer to match the Next.js site. This plan only handles the Next.js side (rewrite config). Astro blog styling is a separate concern.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && grep -q "rewrites\|beforeFiles" next.config.mjs && grep -q "blog\|/blog" src/middleware.ts && echo "PASS"</automated>
    <manual>Start both apps (make dev-hr + make dev-hr-blog) and visit http://localhost:3010/blog to verify proxy works</manual>
  </verify>
  <done>Blog proxy configured in next.config.mjs with beforeFiles rewrites; middleware excludes /blog from locale injection; blog page has fallback content</done>
</task>

<task type="auto">
  <name>Task 2: Complete Bulgarian translations in bg.json</name>
  <files>app/hr/messages/bg.json</files>
  <action>
Create a comprehensive bg.json that mirrors the full en.json structure with Bulgarian translations.

The current bg.json has only ~420 lines while en.json has ~1900 lines. All new content added in plans 02-09 needs Bulgarian translations.

Key sections to translate:
- nav: header navigation items (Features dropdown, HR Tools, Pricing, Blog, Login, Start free trial)
- hero: headline, subheadline, CTAs, trusted companies text
- features overview section (homepage)
- stats section labels
- how-it-works steps
- testimonials (adapt to Bulgarian names and companies)
- blog posts section labels
- cta sections
- footer: all column labels and link text, trust badges, copyright
- pages.features: all feature row content (6 features with titles, descriptions, bullets)
- pages.pricing: module names, descriptions, comparison table, FAQ, quote form labels
- pages.hrTools: index page, all calculator labels and SEO content, template names and descriptions
- pages.hrTools.salaryCalculator: all labels, SEO content about Bulgarian tax system
- pages.hrTools.leaveCalculator: all labels, SEO content about Bulgarian leave laws
- pages.hrTools.employmentCostCalculator: all labels, SEO content
- pages.hrTools.templates: all template names, descriptions, preview content
- pages.about: team section, story, mission, values, stats, CTA
- pages.contact: form labels, contact info, FAQ
- pages.partners: partner types, benefits, form labels
- pages.careers: culture, positions, benefits
- pages.apiDocs: all documentation content
- pages.helpCenter: categories, article titles
- pages.terms, pages.privacy, pages.cookies, pages.gdpr: all legal content in Bulgarian
- pages.systemStatus: service names, status labels
- auth: login/sign-up form labels, left panel text

All Bulgarian text should be natural, professional Bulgarian — not machine-translated. Use proper Bulgarian HR terminology:
- "Подбор на персонал" (recruitment/ATS)
- "Управление на служителите" (employee management)
- "Отпуски" (leave/absence)
- "Заплати" (payroll)
- "Оценка на представянето" (performance reviews)
- "Въвеждане в работа" (onboarding)
- "Безплатен пробен период" (free trial)
- "Поискайте оферта" (get a quote)

NOTE: The bg.json must have EXACTLY the same key structure as en.json. Missing keys will cause runtime errors. Use the en.json as the structural template and translate every value.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && node -e "const en=require('./messages/en.json'); const bg=require('./messages/bg.json'); const enKeys=JSON.stringify(Object.keys(en).sort()); const bgKeys=JSON.stringify(Object.keys(bg).sort()); if(enKeys===bgKeys){console.log('PASS: top-level keys match')}else{console.log('FAIL: key mismatch');process.exit(1)}"</automated>
  </verify>
  <done>bg.json has complete Bulgarian translations matching the full en.json structure; all top-level keys match; content uses natural Bulgarian HR terminology</done>
</task>

</tasks>

<verification>
- Blog proxy works: /blog requests reach the Astro hr-blog
- next-intl middleware does not inject locale prefix on /blog paths
- bg.json has same key structure as en.json
- App builds in both locales without missing translation warnings
- Bulgarian text reads naturally and uses proper HR terminology
</verification>

<success_criteria>
- `bun run build` succeeds
- No missing translation key warnings in build output
- bg.json line count is similar to en.json (within 20%)
- Blog rewrite configuration present in next.config.mjs
</success_criteria>

<output>
After completion, create `.planning/phases/15-hr-app/15-10-SUMMARY.md`
</output>

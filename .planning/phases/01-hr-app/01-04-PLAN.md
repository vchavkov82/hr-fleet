---
phase: 01-hr-app
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/hr/src/app/[locale]/features/page.tsx
  - app/hr/src/components/sections/feature-row.tsx
  - app/hr/messages/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Features page shows alternating rows: screenshot left / text right, then reversed"
    - "Each feature section has title, description, bullet points, and product screenshot placeholder"
    - "At least 6 feature sections covering ATS, employee management, leave, payroll, performance, onboarding"
  artifacts:
    - path: "app/hr/src/components/sections/feature-row.tsx"
      provides: "Alternating layout feature row component"
      contains: "reversed"
    - path: "app/hr/src/app/[locale]/features/page.tsx"
      provides: "Features page with all feature rows"
      contains: "FeatureRow"
  key_links:
    - from: "app/hr/src/app/[locale]/features/page.tsx"
      to: "FeatureRow"
      via: "import from @/components/sections/feature-row"
      pattern: "FeatureRow"
---

<objective>
Build the features page with alternating screenshot/text rows for each HR module.

Purpose: The features page is the main product education page. Alternating rows with product screenshots (placeholders) and detailed descriptions showcase each module of the HR suite.
Output: Full features page with 6 alternating feature rows, hero section, and closing CTA.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hr-app/15-CONTEXT.md
@.planning/phases/15-hr-app/15-01-SUMMARY.md
@app/hr/src/app/[locale]/features/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeatureRow component and redesign features page</name>
  <files>app/hr/src/components/sections/feature-row.tsx, app/hr/src/app/[locale]/features/page.tsx, app/hr/messages/en.json</files>
  <action>
Create app/hr/src/components/sections/feature-row.tsx:
- Server component (no 'use client' needed)
- Props: title (string), description (string), features (string[]), imagePlaceholder (string - label for mockup), reversed (boolean), id (string - for anchor linking from nav)
- Two-column grid: grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center
- Text side: h3 title (text-3xl font-bold font-heading text-navy), p description (text-lg text-gray-600), ul feature list with checkmark SVG icons
- Image side: rounded-2xl border border-gray-200 shadow-lg overflow-hidden bg-gray-50 containing:
  - aspect-[16/10] bg-gradient-to-br from-primary-50 to-primary-100
  - Inside: a realistic-looking UI mockup placeholder — a rounded inner box with sidebar nav + header bar + content area using colored blocks (bg-primary/20, bg-gray-200, etc.) to suggest a dashboard UI
  - Center text: imagePlaceholder label (e.g., "ATS Dashboard") in text-primary/30
- When reversed=true, use lg:order-2 / lg:order-1 to swap columns

Redesign features/page.tsx:
- Keep server component with getTranslations, generateStaticParams, generateMetadata
- Hero section at top: bg-navy-deep text-white py-20, centered h1 + subheading
- Below hero: 6 feature rows with alternating reversed prop:
  1. id="ats" — ATS & Recruitment: job posting, candidate tracking, interview scheduling, team collaboration
  2. id="employees" — Employee Management: employee profiles, org chart, document management, self-service portal
  3. id="leave" — Leave Tracking: leave requests, approval workflows, leave balances, calendar view
  4. id="payroll" — Payroll Integration: payroll processing, tax calculations, payslip generation, bank transfers
  5. id="performance" — Performance Reviews: goal setting, 360 feedback, performance cycles, development plans
  6. id="onboarding" — Onboarding: task checklists, document collection, welcome workflows, mentor assignment
- Each row wrapped in SectionReveal for scroll animation
- Alternating section backgrounds: odd rows white, even rows bg-surface-lighter
- Space between rows: py-20
- Closing CTA section at bottom (import CTA component)

Update en.json pages.features section:
- metaTitle, metaDescription for SEO
- hero.heading, hero.subheading
- For each feature (ats, employees, leave, payroll, performance, onboarding):
  - title, description (2-3 sentences), features (4-5 bullet points)
  - imagePlaceholder label
- All content in English (bg.json will be done in final plan)
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && test -f src/components/sections/feature-row.tsx && grep -q "FeatureRow" src/app/\\[locale\\]/features/page.tsx && grep -q "reversed" src/components/sections/feature-row.tsx && echo "PASS"</automated>
  </verify>
  <done>Features page has 6 alternating feature rows with product screenshot placeholders, hero section, SectionReveal animations, and closing CTA</done>
</task>

</tasks>

<verification>
- Features page renders 6 feature sections with alternating layouts
- Each feature row has title, description, bullet list, and screenshot placeholder
- Anchor IDs (#ats, #employees, #leave, #payroll, #performance, #onboarding) work for header nav links
- SectionReveal wraps each feature row
- CTA at bottom
</verification>

<success_criteria>
- `bun run build` succeeds
- Features page renders all 6 sections without errors
- Alternating layout (image left/right) works correctly on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/15-hr-app/15-04-SUMMARY.md`
</output>

---
phase: 01-hr-app
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/hr/src/components/layout/header.tsx
  - app/hr/src/components/layout/footer.tsx
  - app/hr/src/components/sections/hero.tsx
  - app/hr/messages/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Header has sticky 'Start free trial' CTA button and updated nav structure (Features dropdown, Pricing, HR Tools, Blog)"
    - "Footer has corporate feel with updated branding and link structure"
    - "Homepage hero has product screenshot layout (text left, screenshot right)"
    - "Header CTA says 'Start free trial' (not 'Book a Demo')"
  artifacts:
    - path: "app/hr/src/components/layout/header.tsx"
      provides: "Updated header with free trial CTA and corporate nav"
      contains: "startFree"
    - path: "app/hr/src/components/sections/hero.tsx"
      provides: "Product screenshot hero with left text, right mockup"
      contains: "grid"
  key_links:
    - from: "app/hr/src/components/layout/header.tsx"
      to: "/auth/sign-up"
      via: "Link href"
      pattern: "auth/sign-up"
---

<objective>
Redesign the header, footer, and homepage hero to match the corporate blue design language with product-screenshot hero layout.

Purpose: The layout (header/footer) wraps every page — updating them first ensures all subsequent page work inherits the correct design. The hero is the first thing visitors see.
Output: Updated header with "Start free trial" CTA, corporate footer, product-screenshot hero section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hr-app/15-CONTEXT.md
@.planning/phases/15-hr-app/15-01-SUMMARY.md
@app/hr/src/components/layout/header.tsx
@app/hr/src/components/layout/footer.tsx
@app/hr/src/components/sections/hero.tsx
@app/hr/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign header and footer with corporate branding and free trial CTA</name>
  <files>app/hr/src/components/layout/header.tsx, app/hr/src/components/layout/footer.tsx, app/hr/messages/en.json</files>
  <action>
Update header.tsx:
- Keep 'use client' and existing structure (NavDropdown, LanguageSwitcher, mobile menu)
- Update nav items: Platform dropdown -> Features dropdown (items: ATS/Recruitment, Employee Management, Leave Tracking, Payroll, Performance Reviews, Onboarding)
- Keep Pricing as direct link
- Add HR Tools as direct link (/hr-tools)
- Keep Blog as direct link
- Remove "Book a Demo" CTA button entirely
- Primary header CTA: "Start free trial" → links to /auth/sign-up, use bg-primary text-white styling
- Secondary: "Login" text link to /auth/login
- Mobile menu: same structure, "Start free trial" as primary CTA at bottom
- Update logo text: keep "Jobs HR" with the gradient HR badge

Update footer.tsx:
- Update column structure for HR SaaS:
  - Product: ATS, Employee Management, Leave Tracking, Payroll, Performance Reviews → /features#section-id
  - Resources: Blog (/blog), HR Tools (/hr-tools), Help Center (/help-center), API Docs (/api-docs)
  - Company: About (/about), Careers (/careers), Partners (/partners), Contact (/contact)
  - Legal: Terms (/terms), Privacy (/privacy), Cookies (/cookies), GDPR (/gdpr)
- Keep social links, trust badges (SOC 2, GDPR, ISO 27001)
- Use new navy.DEFAULT (#0F172A) for footer background

Update en.json nav section:
- Update nav.platform -> nav.features with new dropdown items
- Update nav.bookDemo -> nav.startFree = "Start free trial"
- Update footer column labels and link labels to match new structure
- Remove any demo-related strings, replace with free trial language throughout
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && grep -q "startFree\|start.*free\|Start free" src/components/layout/header.tsx && echo "PASS"</automated>
  </verify>
  <done>Header has "Start free trial" as primary CTA, Features dropdown with HR modules, no "Book a Demo"; footer has updated HR SaaS column structure</done>
</task>

<task type="auto">
  <name>Task 2: Redesign homepage hero with product screenshot layout</name>
  <files>app/hr/src/components/sections/hero.tsx, app/hr/messages/en.json</files>
  <action>
Redesign hero.tsx:
- Keep as async server component using getTranslations('hero')
- Change layout from centered text + stat cards to a two-column grid:
  - Left column (text): badge, h1 headline, p subheadline, CTA buttons row ("Start free trial" primary + "See features" secondary)
  - Right column (product screenshot mockup): a browser chrome frame (rounded-2xl border shadow) containing a gradient placeholder representing a product dashboard screenshot. Use a professional-looking UI mockup placeholder: rounded-2xl bg-white border border-gray-200 shadow-2xl with a fake dashboard layout inside (sidebar + header + content grid with colored blocks representing charts/tables)
- Desktop: grid-cols-2 with items-center gap-16
- Mobile: single column, text first, mockup below
- Below the hero grid: trusted companies strip — a row of placeholder company name text items (6-8) in grayscale, with "Trusted by 500+ Bulgarian companies" label above
- Move stat counters to a separate section (they'll be used later with the Counter component)

Update en.json hero section:
- hero.headline1: "Управлявайте HR екипа си от едно място" (or English: "Manage your entire HR from one place")
- hero.headline2: "с лекота и увереност" (or English: "with ease and confidence")
- hero.subheadline: Updated to focus on Bulgarian SMB value prop
- hero.ctaStart: "Start free trial" / "Започнете безплатно"
- hero.ctaFeatures: "See features" / "Вижте функциите"
- hero.trustedBy: "Trusted by 500+ Bulgarian companies"
- Add hero.companies as array of placeholder company names

Keep using Link from @/navigation for CTAs. Use native <a> tags for external links only.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && grep -q "grid-cols" src/components/sections/hero.tsx && grep -q "trusted\|Trusted\|trustedBy" src/components/sections/hero.tsx && echo "PASS"</automated>
  </verify>
  <done>Hero has two-column layout with text left + product mockup right, trusted companies strip below, "Start free trial" as primary CTA</done>
</task>

</tasks>

<verification>
- Header shows "Start free trial" button (not "Book a Demo")
- Header nav has Features dropdown with HR module items
- Footer has HR SaaS column structure (Product, Resources, Company, Legal)
- Hero is two-column: text + CTA left, product screenshot mockup right
- Trusted companies strip appears below hero
- All text goes through t() translations
</verification>

<success_criteria>
- `bun run build` succeeds
- Header renders without console errors
- Hero renders two-column layout on desktop
- No hardcoded text strings outside of i18n
</success_criteria>

<output>
After completion, create `.planning/phases/15-hr-app/15-02-SUMMARY.md`
</output>

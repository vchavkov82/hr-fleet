---
phase: 01-hr-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/hr/tailwind.config.js
  - app/hr/src/app/globals.css
  - app/hr/src/components/ui/section-reveal.tsx
  - app/hr/src/components/ui/counter.tsx
  - app/hr/src/lib/bulgarian-tax.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Tailwind palette uses deeper corporate blue (#1B4DDB primary) instead of bright #215cff"
    - "SectionReveal and Counter client components exist and can be imported"
    - "Bulgarian tax rate constants module exists with 2026 rates"
  artifacts:
    - path: "app/hr/tailwind.config.js"
      provides: "Corporate blue palette, navy deep, accent sky, surface colors"
      contains: "#1B4DDB"
    - path: "app/hr/src/components/ui/section-reveal.tsx"
      provides: "framer-motion whileInView scroll reveal wrapper"
      contains: "use client"
    - path: "app/hr/src/components/ui/counter.tsx"
      provides: "Animated number counter with useInView"
      contains: "use client"
    - path: "app/hr/src/lib/bulgarian-tax.ts"
      provides: "BG_TAX_2026 constants for calculators"
      contains: "INCOME_TAX_RATE"
  key_links:
    - from: "app/hr/src/components/ui/section-reveal.tsx"
      to: "framer-motion"
      via: "import { motion } from 'framer-motion'"
      pattern: "from 'framer-motion'"
    - from: "app/hr/src/components/ui/counter.tsx"
      to: "framer-motion"
      via: "useInView, useSpring, useMotionValue"
      pattern: "from 'framer-motion'"
---

<objective>
Establish the visual design system foundation for the HR app corporate redesign and create reusable UI primitives.

Purpose: All subsequent plans depend on the updated color palette, animation primitives, and tax constants being in place. This is the foundation wave that unblocks everything else.
Output: Updated tailwind config with corporate blue palette, SectionReveal + Counter client components, Bulgarian tax rate constants module, updated globals.css button/card classes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hr-app/15-CONTEXT.md
@.planning/phases/15-hr-app/15-RESEARCH.md
@app/hr/tailwind.config.js
@app/hr/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tailwind palette and globals.css to corporate blue design system</name>
  <files>app/hr/tailwind.config.js, app/hr/src/app/globals.css</files>
  <action>
Update tailwind.config.js colors to the corporate blue palette (per research recommendations):
- primary.DEFAULT: '#1B4DDB' (was #215cff — deeper corporate blue)
- primary.light: '#3B6EF0', primary.dark: '#133AAB'
- primary.50: '#EEF2FF', primary.100: '#DBE4FE', primary.200: '#BCC8FC', primary.500: '#1B4DDB', primary.600: '#133AAB', primary.700: '#0E2E80', primary.900: '#0A1F5C'
- accent.DEFAULT: '#0EA5E9' (sky blue, replaces pink #EC4899 — more corporate), accent.light: '#38BDF8', accent.dark: '#0284C7'
- navy.DEFAULT: '#0F172A' (deeper, was #1E293B), navy.light: '#1E293B', navy.dark: '#020617', navy.deep: '#030B24'
- success: '#059669', warning: '#D97706'
- surface.light: '#F0F4FE', surface.lighter: '#F8FAFC'

Update background gradients to match new palette:
- hero-gradient: keep light blue to white
- brand-gradient: update to use new primary shades
- cta-gradient: update to new primary shades

Update globals.css:
- .btn-primary: update to use new corporate blue (bg-primary, hover:bg-primary-dark)
- .btn-secondary: border-primary styling
- .card: ensure shadow/border styles are consistent with corporate feel
- .section-label: keep inline badge style
- .container-xl: ensure max-w-7xl mx-auto px-4 sm:px-6 lg:px-8

Do NOT change font families (Inter + Mulish are good). Do NOT add dark mode styles.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && grep -q "1B4DDB" tailwind.config.js && grep -q "0EA5E9" tailwind.config.js && echo "PASS"</automated>
    <manual>Visually verify the color palette looks professional and corporate</manual>
  </verify>
  <done>Tailwind config has corporate blue palette with all required color tokens; globals.css button/card classes updated to match new palette</done>
</task>

<task type="auto">
  <name>Task 2: Create UI primitives (SectionReveal, Counter) and Bulgarian tax constants</name>
  <files>app/hr/src/components/ui/section-reveal.tsx, app/hr/src/components/ui/counter.tsx, app/hr/src/lib/bulgarian-tax.ts</files>
  <action>
Create app/hr/src/components/ui/section-reveal.tsx:
- 'use client' directive
- import { motion, type Variants } from 'framer-motion'
- Define fadeUp variant: hidden = { opacity: 0, y: 30 }, visible = { opacity: 1, y: 0 }
- SectionReveal component accepts children, className, delay (default 0)
- Uses motion.div with variants, initial="hidden", whileInView="visible"
- viewport={{ once: true, margin: '-80px' }}
- transition={{ duration: 0.6, delay, ease: [0.25, 0.46, 0.45, 0.94] }}

Create app/hr/src/components/ui/counter.tsx:
- 'use client' directive
- import { useInView, useMotionValue, useSpring } from 'framer-motion'
- Counter component: { target: number, suffix?: string, prefix?: string, className?: string }
- Uses useRef, useInView({ once: true }), useMotionValue(0), useSpring with duration: 2000, bounce: 0
- useEffect triggers motionValue.set(target) when isInView
- Renders motion.span with ref and formatted display (Math.round + toLocaleString)

Create app/hr/src/lib/bulgarian-tax.ts:
- Export const BG_TAX_2026 following the exact structure from research:
  - INCOME_TAX_RATE: 0.10
  - SOCIAL_SECURITY with pension/illness/maternity/unemployment splits (employer/employee)
  - ACCIDENT_EMPLOYER: 0.005
  - HEALTH with total/employer/employee (8% split)
  - UNIVERSAL_PENSION with total/employer/employee (5% split)
  - MIN_INSURABLE_INCOME: 1213, MAX_INSURABLE_INCOME: 3850
  - TOTAL_EMPLOYER_RATE: 0.1892, TOTAL_EMPLOYEE_RATE: 0.1378
  - MINIMUM_ANNUAL_LEAVE_DAYS: 20, PUBLIC_HOLIDAYS_PER_YEAR: 15
- Use 'as const' for type safety
- Add JSDoc comment citing sources (PWC Tax Summaries Bulgaria 2026)
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && test -f src/components/ui/section-reveal.tsx && test -f src/components/ui/counter.tsx && test -f src/lib/bulgarian-tax.ts && grep -q "use client" src/components/ui/section-reveal.tsx && grep -q "BG_TAX_2026" src/lib/bulgarian-tax.ts && echo "PASS"</automated>
  </verify>
  <done>SectionReveal whileInView wrapper, Counter animated number component, and BG_TAX_2026 constants all exist with correct implementations</done>
</task>

</tasks>

<verification>
- tailwind.config.js contains new corporate palette (#1B4DDB primary, #0EA5E9 accent, #0F172A navy)
- globals.css button classes (.btn-primary, .btn-secondary) use new palette
- SectionReveal uses framer-motion whileInView with fade-up animation
- Counter uses useInView + useSpring for scroll-triggered counting
- BG_TAX_2026 has all required tax rate fields for calculators
</verification>

<success_criteria>
- `bun run build` in app/hr succeeds (no TypeScript errors from new files)
- grep confirms #1B4DDB in tailwind config
- All three new files (section-reveal, counter, bulgarian-tax) exist and export correctly
</success_criteria>

<output>
After completion, create `.planning/phases/15-hr-app/15-01-SUMMARY.md`
</output>

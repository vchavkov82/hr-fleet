---
phase: 01-hr-app
plan: 07
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/hr/src/app/[locale]/hr-tools/templates/page.tsx
  - app/hr/src/app/[locale]/hr-tools/templates/employment-contract/page.tsx
  - app/hr/src/app/[locale]/hr-tools/templates/job-description/page.tsx
  - app/hr/src/app/[locale]/hr-tools/templates/leave-policy/page.tsx
  - app/hr/src/app/[locale]/hr-tools/templates/onboarding-checklist/page.tsx
  - app/hr/src/app/[locale]/hr-tools/templates/performance-review/page.tsx
  - app/hr/src/app/[locale]/hr-tools/templates/termination-document/page.tsx
  - app/hr/src/components/ui/email-gate-modal.tsx
  - app/hr/messages/en.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Each template has its own URL under /hr-tools/templates/*"
    - "Template download requires email submission (email gate)"
    - "Templates index page lists all 6 templates with descriptions"
    - "Template pages show preview content and download button"
  artifacts:
    - path: "app/hr/src/components/ui/email-gate-modal.tsx"
      provides: "Email capture modal for template downloads"
      contains: "use client"
    - path: "app/hr/src/app/[locale]/hr-tools/templates/page.tsx"
      provides: "Templates listing page"
      contains: "template"
    - path: "app/hr/src/app/[locale]/hr-tools/templates/employment-contract/page.tsx"
      provides: "Employment contract template page"
      contains: "email"
  key_links:
    - from: "app/hr/src/app/[locale]/hr-tools/templates/employment-contract/page.tsx"
      to: "EmailGateModal"
      via: "import from @/components/ui/email-gate-modal"
      pattern: "EmailGateModal"
---

<objective>
Build the email-gated HR template download system with individual pages for each template.

Purpose: Templates are lead-generation assets. Each template has its own SEO-optimized page with preview content and requires email submission before download. This captures leads while providing genuine value to HR managers.
Output: 6 template pages on individual URLs, email gate modal, templates index page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hr-app/15-CONTEXT.md
@.planning/phases/15-hr-app/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmailGateModal component and templates index page</name>
  <files>app/hr/src/components/ui/email-gate-modal.tsx, app/hr/src/app/[locale]/hr-tools/templates/page.tsx, app/hr/messages/en.json</files>
  <action>
Create app/hr/src/components/ui/email-gate-modal.tsx:
- 'use client' directive
- import { useState } from 'react'
- import { useForm } from 'react-hook-form'
- import { zodResolver } from '@hookform/resolvers/zod'
- import { z } from 'zod'
- import { Dialog } from '@headlessui/react' (for accessible modal)
- Schema: { email: z.string().email(), name: z.string().min(2) }
- Props: { isOpen: boolean, onClose: () => void, templateSlug: string, templateName: string, labels: Record<string, string> }
- On submit:
  - Store in localStorage: key 'template_leads', append { email, name, templateSlug, timestamp }
  - Also set localStorage key `template_unlocked_${templateSlug}` = 'true'
  - Call onClose()
  - Trigger download by programmatically clicking a hidden <a> link to /templates/{slug}.pdf
- UI: Dialog overlay, white modal with:
  - Heading: "Download {templateName}"
  - Subtext: "Enter your email to get free access"
  - Name input, Email input
  - Submit button: "Download free template"
  - Close X button

Create app/hr/src/app/[locale]/hr-tools/templates/page.tsx:
- Server component with getTranslations, generateStaticParams, generateMetadata
- Hero section: bg-navy-deep text-white, "Free HR Document Templates"
- Grid of 6 template cards (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6):
  1. Employment Contract Template
  2. Job Description Template
  3. Leave Policy Template
  4. Onboarding Checklist
  5. Performance Review Form
  6. Termination Document
- Each card: rounded-2xl border border-gray-200 p-6 shadow-sm
  - Document icon (SVG), template name (h3), description, format badge (PDF)
  - "Download free →" link to /hr-tools/templates/{slug}
- Below grid: CTA section "Need custom templates? Start your free trial"

Update en.json with pages.hrTools.templates section: metaTitle, metaDescription, heading, subheading, and array of 6 templates with name/description/slug.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && test -f src/components/ui/email-gate-modal.tsx && test -f src/app/\\[locale\\]/hr-tools/templates/page.tsx && grep -q "headlessui\|Dialog" src/components/ui/email-gate-modal.tsx && echo "PASS"</automated>
  </verify>
  <done>EmailGateModal captures email with accessible Dialog; templates index lists all 6 templates with links to individual pages</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 individual template pages with email-gated downloads</name>
  <files>app/hr/src/app/[locale]/hr-tools/templates/employment-contract/page.tsx, app/hr/src/app/[locale]/hr-tools/templates/job-description/page.tsx, app/hr/src/app/[locale]/hr-tools/templates/leave-policy/page.tsx, app/hr/src/app/[locale]/hr-tools/templates/onboarding-checklist/page.tsx, app/hr/src/app/[locale]/hr-tools/templates/performance-review/page.tsx, app/hr/src/app/[locale]/hr-tools/templates/termination-document/page.tsx, app/hr/messages/en.json</files>
  <action>
Create a reusable template page pattern. Each page follows the same structure:

For each of the 6 templates, create page.tsx as server component:
- getTranslations, generateStaticParams (locales), generateMetadata
- Layout:
  1. Hero: bg-navy-deep text-white py-16, template icon + title + format badge
  2. Content grid: grid-cols-1 lg:grid-cols-3 gap-8
     - Left column (lg:col-span-2): template preview content
       - "What's included" section with bullet list of template sections
       - "How to use this template" guide with numbered steps
       - SEO content about the relevant Bulgarian labor law / HR topic
     - Right column (lg:col-span-1): sticky download card
       - Template thumbnail (gradient placeholder)
       - Format: PDF
       - "Download free" button — triggers EmailGateModal
       - If already unlocked (check localStorage), show direct "Download again" link
  3. Related templates section: 3 other template cards linking to their pages

Create a shared TemplateDownloadCard client component for the right sidebar:
- 'use client'
- Checks localStorage for `template_unlocked_${slug}`
- If unlocked: shows download link
- If not: shows "Download free" button that opens EmailGateModal
- Imports and renders EmailGateModal

Template-specific content for each (in en.json):
1. employment-contract: Bulgarian labor code requirements, essential clauses, probation period rules
2. job-description: Structure of effective JD, Bulgarian market specifics, salary range guidance
3. leave-policy: Types of leave in Bulgaria (annual, sick, maternity, paternity), legal minimums
4. onboarding-checklist: First day/week/month tasks, document checklist, IT setup, mentor assignment
5. performance-review: 360 feedback structure, goal setting, Bulgarian HR best practices
6. termination-document: Termination notice requirements, severance rules, Bulgarian labor code articles

NOTE: Actual PDF files don't need to exist — the download attempt is gated by email, and the button can show a "Template will be emailed to you" message after email submission (no real PDF in /public/templates/ needed).

Update en.json with detailed content for each template page.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/jobs/app/hr && test -f src/app/\\[locale\\]/hr-tools/templates/employment-contract/page.tsx && test -f src/app/\\[locale\\]/hr-tools/templates/job-description/page.tsx && test -f src/app/\\[locale\\]/hr-tools/templates/leave-policy/page.tsx && test -f src/app/\\[locale\\]/hr-tools/templates/onboarding-checklist/page.tsx && test -f src/app/\\[locale\\]/hr-tools/templates/performance-review/page.tsx && test -f src/app/\\[locale\\]/hr-tools/templates/termination-document/page.tsx && echo "PASS"</automated>
  </verify>
  <done>All 6 template pages exist at individual URLs with email-gated download, preview content, SEO text about Bulgarian HR laws, and related templates section</done>
</task>

</tasks>

<verification>
- All 6 template pages accessible at /hr-tools/templates/{slug}
- Each template page has SEO content, download card, and related templates
- EmailGateModal captures email before allowing download
- localStorage stores lead data on submission
- Templates index lists all 6 with links
- All pages have generateStaticParams and generateMetadata
</verification>

<success_criteria>
- `bun run build` succeeds
- All 6 template routes resolve without 404
- EmailGateModal opens on "Download free" click
- After email submission, template is marked as unlocked in localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/15-hr-app/15-07-SUMMARY.md`
</output>

---
phase: 06-core-go-rest-api
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - services/api/internal/handler/employee.go
  - services/api/internal/service/employee.go
  - services/api/platform/odoo/contract.go
  - services/api/platform/odoo/leave.go
  - services/api/platform/odoo/types.go
  - services/api/internal/handler/contract.go
  - services/api/internal/service/contract.go
  - services/api/internal/handler/leave.go
  - services/api/internal/service/leave.go
autonomous: true
requirements:
  - API-03
  - API-06
  - API-07

must_haves:
  truths:
    - "Employee CRUD has soft delete, pagination, and filtering"
    - "Contract endpoints list and create employment contracts via Odoo"
    - "Leave endpoints support allocations, requests, approve/reject workflow"
    - "Contract creation, leave approve/reject, and employee updates are recorded in the audit log"
    - "Webhook events are dispatched for employee.created, employee.updated, leave.approved, leave.rejected"
  artifacts:
    - path: "services/api/internal/handler/contract.go"
      provides: "Contract CRUD endpoints"
    - path: "services/api/internal/handler/leave.go"
      provides: "Leave management endpoints"
    - path: "services/api/platform/odoo/contract.go"
      provides: "Odoo hr.contract integration"
    - path: "services/api/platform/odoo/leave.go"
      provides: "Odoo hr.leave and hr.leave.allocation integration"
  key_links:
    - from: "services/api/internal/handler/contract.go"
      to: "services/api/internal/service/contract.go"
      via: "handler -> service pattern"
      pattern: "svc\\."
    - from: "services/api/internal/service/leave.go"
      to: "services/api/platform/odoo/leave.go"
      via: "service -> odoo"
      pattern: "odoo\\."
    - from: "services/api/internal/service/contract.go"
      to: "services/api/internal/db/queries/audit.sql"
      via: "Audit log write on contract creation"
      pattern: "CreateAuditEntry"
    - from: "services/api/internal/service/leave.go"
      to: "services/api/internal/db/queries/audit.sql"
      via: "Audit log write on leave approve/reject"
      pattern: "CreateAuditEntry"
    - from: "services/api/internal/service/employee.go"
      to: "services/api/internal/service/webhook.go"
      via: "Webhook dispatch on employee create/update"
      pattern: "webhookSvc\\.Dispatch"
    - from: "services/api/internal/service/leave.go"
      to: "services/api/internal/service/webhook.go"
      via: "Webhook dispatch on leave approve/reject"
      pattern: "webhookSvc\\.Dispatch"
---

<objective>
Enhance employee CRUD (soft delete, filtering), add contract endpoints (list/create via Odoo hr.contract), and leave management endpoints (allocations, requests, approve/reject via Odoo hr.leave). Wire audit logging and webhook dispatch for sensitive operations.

Purpose: Core HR domain endpoints that the Admin UI will consume — employee, contract, and leave management.
Output: Working employee, contract, and leave handlers following existing handler->service->odoo pattern, with audit trail and webhook events.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-core-go-rest-api/06-RESEARCH.md
@services/api/internal/handler/employee.go
@services/api/internal/service/employee.go
@services/api/platform/odoo/employee.go
@services/api/platform/odoo/types.go

<interfaces>
<!-- From 06-02 (sqlc generated) -->
```go
func (q *Queries) CreateAuditEntry(ctx, params CreateAuditEntryParams) error
// CreateAuditEntryParams: UserID uuid.UUID, Action string, ResourceType string, ResourceID string, Details json.RawMessage
```

<!-- From 06-06 (webhook service) -->
```go
// WebhookService.Dispatch will be available — accepts event name and payload
// If WebhookService is not yet available at build time, accept it as an optional dependency (interface)
type WebhookDispatcher interface {
    Dispatch(ctx context.Context, event string, payload any) error
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance employee CRUD + contract endpoints</name>
  <files>services/api/internal/handler/employee.go, services/api/internal/service/employee.go, services/api/platform/odoo/contract.go, services/api/platform/odoo/types.go, services/api/internal/handler/contract.go, services/api/internal/service/contract.go</files>
  <action>
1. Enhance `handler/employee.go`:
   - Add `?search=`, `?department=`, `?active=` query params for filtering
   - Add `?page=` and `?per_page=` with defaults (1, 20)
   - Add `DELETE /employees/{id}` for soft delete (sets active=false in Odoo)
   - Use RespondList for paginated responses
   - Use RespondError for structured errors
   - Add RBAC: ManageEmployees for write, ViewEmployees for read

2. Enhance `service/employee.go`:
   - Accept a `WebhookDispatcher` interface as optional dependency (can be nil if not yet wired)
   - In `Create`: after successful Odoo creation, call `db.CreateAuditEntry(ctx, {Action: "employee.created", ResourceType: "employee", ResourceID: employeeID, Details: summary})` and call `webhookSvc.Dispatch(ctx, "employee.created", employeePayload)` if webhookSvc is not nil
   - In `Update`: after successful Odoo update, call `db.CreateAuditEntry(ctx, {Action: "employee.updated", ...})` and call `webhookSvc.Dispatch(ctx, "employee.updated", employeePayload)` if webhookSvc is not nil

3. Add to `platform/odoo/types.go`:
   - `Contract` struct: ID, EmployeeID Many2One, Name, DateStart, DateEnd, Wage float64, State string, StructureType Many2One, Department Many2One
   - Map from Odoo `hr.contract` fields

4. Create `platform/odoo/contract.go`:
   - `SearchContracts(ctx, filters, offset, limit) ([]Contract, int, error)` — search_read on hr.contract
   - `CreateContract(ctx, vals map[string]any) (int, error)` — create on hr.contract
   - Follow existing employee.go pattern for field mapping

5. Create `internal/service/contract.go`:
   - `ContractService` struct with odoo client, cache, db queries
   - `List(ctx, employeeID, page, perPage)` — cached read
   - `Create(ctx, contract)` — synchronous write to Odoo (per user decision). **After successful creation, call `db.CreateAuditEntry(ctx, {Action: "contract.created", ResourceType: "contract", ResourceID: contractID, Details: contract summary})`**

6. Create `internal/handler/contract.go`:
   - `GET /api/v1/contracts?employee_id=&page=&per_page=` — list contracts
   - `POST /api/v1/contracts` — create contract
   - `GET /api/v1/contracts/{id}` — get single contract
   - RBAC: ManageContracts for write, any authenticated for read
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/services/api && go build ./internal/handler/... ./internal/service/... ./platform/odoo/...</automated>
  </verify>
  <done>Employee CRUD enhanced with soft delete/filtering/pagination, contract CRUD via Odoo compiles, audit entries written for employee create/update and contract create, webhook events dispatched for employee.created and employee.updated</done>
</task>

<task type="auto">
  <name>Task 2: Leave management endpoints with audit and webhook dispatch</name>
  <files>services/api/platform/odoo/leave.go, services/api/internal/handler/leave.go, services/api/internal/service/leave.go</files>
  <action>
1. Create `platform/odoo/leave.go`:
   - `LeaveAllocation` struct: ID, EmployeeID Many2One, HolidayStatusID Many2One (leave type), NumberOfDays float64, State string
   - `LeaveRequest` struct: ID, EmployeeID Many2One, HolidayStatusID Many2One, DateFrom, DateTo time.Time, NumberOfDays float64, State string, Name string
   - `SearchLeaveAllocations(ctx, filters, offset, limit)` — hr.leave.allocation search_read
   - `SearchLeaveRequests(ctx, filters, offset, limit)` — hr.leave search_read
   - `CreateLeaveRequest(ctx, vals)` — hr.leave create
   - `ActionApproveLeave(ctx, leaveID)` — call action_approve on hr.leave (Odoo RPC method call)
   - `ActionRefuseLeave(ctx, leaveID)` — call action_refuse on hr.leave

2. Create `internal/service/leave.go`:
   - `LeaveService` struct with odoo, cache, db queries, webhookSvc (WebhookDispatcher interface, can be nil)
   - `ListAllocations(ctx, employeeID, page, perPage)`
   - `ListRequests(ctx, employeeID, status, page, perPage)`
   - `CreateRequest(ctx, req)` — sync write to Odoo
   - `ApproveRequest(ctx, leaveID, approverUserID)` — sync to Odoo, requires ApproveLeave permission. **After successful approval, call `db.CreateAuditEntry(ctx, {UserID: approverUserID, Action: "leave.approved", ResourceType: "leave_request", ResourceID: leaveID, Details: leave summary})` and call `webhookSvc.Dispatch(ctx, "leave.approved", leavePayload)` if webhookSvc is not nil.**
   - `RejectRequest(ctx, leaveID, reason, rejecterUserID)` — sync to Odoo. **After successful rejection, call `db.CreateAuditEntry(ctx, {UserID: rejecterUserID, Action: "leave.rejected", ResourceType: "leave_request", ResourceID: leaveID, Details: {reason: reason}})` and call `webhookSvc.Dispatch(ctx, "leave.rejected", leavePayload)` if webhookSvc is not nil.**

3. Create `internal/handler/leave.go`:
   - `GET /api/v1/leave/allocations?employee_id=` — list allocations
   - `GET /api/v1/leave/requests?employee_id=&status=` — list leave requests
   - `POST /api/v1/leave/requests` — create leave request
   - `POST /api/v1/leave/requests/{id}/approve` — approve (RBAC: ApproveLeave)
   - `POST /api/v1/leave/requests/{id}/reject` — reject (RBAC: ApproveLeave)
   - Use RespondJSON/RespondError, RespondList for pagination
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/services/api && go build ./internal/handler/... ./internal/service/... ./platform/odoo/...</automated>
  </verify>
  <done>Leave allocation listing, request creation, approve/reject endpoints compile, follow handler->service->odoo pattern, audit entries written for approve/reject, webhook events dispatched for leave.approved and leave.rejected</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds
- Employee, contract, leave handlers all follow consistent pattern
- grep confirms CreateAuditEntry calls in contract and leave services
- grep confirms webhookSvc.Dispatch calls in employee and leave services
</verification>

<success_criteria>
- Employee CRUD has soft delete, search/filter, pagination
- Contract list/create via Odoo hr.contract
- Leave allocations, requests, approve/reject via Odoo hr.leave
- All endpoints use structured responses and RBAC
- Audit log entries written for: employee.created, employee.updated, contract.created, leave.approved, leave.rejected
- Webhook events dispatched for: employee.created, employee.updated, leave.approved, leave.rejected
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-go-rest-api/06-04-SUMMARY.md`
</output>

---
phase: 06-core-go-rest-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/migrations/002_users_auth.sql
  - services/api/migrations/003_payroll_runs.sql
  - services/api/migrations/004_webhooks.sql
  - services/api/internal/db/queries/users.sql
  - services/api/internal/db/queries/payroll.sql
  - services/api/internal/db/queries/webhooks.sql
  - services/api/internal/db/queries/audit.sql
  - services/api/sqlc.yaml
autonomous: true
requirements:
  - API-02
  - API-04
  - API-05
  - API-09

must_haves:
  truths:
    - "Database schema supports users, API keys, refresh tokens, payroll runs, payslips, webhooks, audit log"
    - "sqlc generates type-safe Go code for all queries"
  artifacts:
    - path: "services/api/migrations/002_users_auth.sql"
      provides: "Users, refresh_tokens, api_keys tables"
      contains: "CREATE TABLE users"
    - path: "services/api/migrations/003_payroll_runs.sql"
      provides: "Payroll runs, payslips, audit_log tables"
      contains: "CREATE TABLE payroll_runs"
    - path: "services/api/migrations/004_webhooks.sql"
      provides: "Webhook registrations and deliveries tables"
      contains: "CREATE TABLE webhook_registrations"
  key_links:
    - from: "services/api/internal/db/queries/users.sql"
      to: "migrations/002_users_auth.sql"
      via: "sqlc queries reference users table"
      pattern: "FROM users"
---

<objective>
Create database migrations for auth, payroll, webhooks, and audit tables. Write sqlc query files and generate type-safe Go code.

Purpose: Database foundation that auth, payroll, and webhook plans depend on.
Output: Migrations applied, sqlc-generated Go code for all domain queries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-go-rest-api/06-RESEARCH.md
@services/api/migrations/001_initial.sql
@services/api/sqlc.yaml
@services/api/internal/db/db.go
@services/api/internal/db/models.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migrations</name>
  <files>services/api/migrations/002_users_auth.sql, services/api/migrations/003_payroll_runs.sql, services/api/migrations/004_webhooks.sql</files>
  <action>
1. Create `migrations/002_users_auth.sql` — exact schema from RESEARCH.md:
   - `users` table: id UUID PK, email UNIQUE, password_hash, role (CHECK constraint for 7 roles), active, timestamps
   - `refresh_tokens` table: id UUID PK, user_id FK, token_hash UNIQUE, expires_at, created_at
   - `api_keys` table: id UUID PK, user_id FK, name, key_hash UNIQUE, key_prefix, scopes TEXT[], active, last_used_at, expires_at, created_at
   - Indexes on users(email), refresh_tokens(token_hash), api_keys(key_hash)

2. Create `migrations/003_payroll_runs.sql`:
   - `payroll_runs` table: id UUID PK, period_start DATE, period_end DATE, status (CHECK: draft/approved/processing/completed/failed), created_by FK, approved_by FK nullable, error_details JSONB, completed_at, timestamps
   - `payslips` table: id UUID PK, payroll_run_id FK, employee_odoo_id INT, gross_salary_stotinki BIGINT, employer_social/employee_social/employer_health/employee_health/income_tax/net_salary_stotinki all BIGINT, calculation_details JSONB, created_at
   - `audit_log` table: id BIGSERIAL PK, user_id FK nullable, action TEXT, resource_type TEXT, resource_id TEXT, details JSONB, created_at
   - Index on payroll_runs(status), payslips(payroll_run_id), audit_log(resource_type, resource_id)

3. Create `migrations/004_webhooks.sql`:
   - `webhook_registrations`: id UUID PK, url TEXT, events TEXT[], secret TEXT, active BOOLEAN, created_by FK, created_at
   - `webhook_deliveries`: id UUID PK, webhook_id FK, event TEXT, payload JSONB, status TEXT default 'pending', attempts INT, last_response_code INT, last_error TEXT, created_at
  </action>
  <verify>
    <automated>ls -la /home/vchavkov/src/hr/services/api/migrations/00*.sql && echo "Migration files exist"</automated>
  </verify>
  <done>All 3 migration files created with correct schema, constraints, and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Write sqlc queries and generate Go code</name>
  <files>services/api/internal/db/queries/users.sql, services/api/internal/db/queries/payroll.sql, services/api/internal/db/queries/webhooks.sql, services/api/internal/db/queries/audit.sql, services/api/sqlc.yaml</files>
  <action>
1. Update `sqlc.yaml` to include the new query files (check existing config pattern for queries path).

2. Create `internal/db/queries/users.sql`:
   - CreateUser, GetUserByEmail, GetUserByID, UpdateUserRole, DeactivateUser
   - CreateRefreshToken, GetRefreshToken, DeleteRefreshToken, DeleteExpiredRefreshTokens
   - CreateAPIKey, GetAPIKeyByHash, ListAPIKeysByUser, DeactivateAPIKey, UpdateAPIKeyLastUsed

3. Create `internal/db/queries/payroll.sql`:
   - CreatePayrollRun, GetPayrollRun, ListPayrollRuns (with status filter, pagination), UpdatePayrollRunStatus, SetPayrollRunError, SetPayrollRunCompleted
   - CreatePayslip, ListPayslipsByRun, GetPayslip
   - BatchCreatePayslips (use unnest or loop pattern for sqlc)

4. Create `internal/db/queries/webhooks.sql`:
   - CreateWebhookRegistration, ListWebhookRegistrations, GetWebhookRegistration, DeactivateWebhook
   - CreateWebhookDelivery, UpdateWebhookDeliveryStatus, ListPendingDeliveries, ListDeliveriesByWebhook

5. Create `internal/db/queries/audit.sql`:
   - CreateAuditEntry, ListAuditEntries (with resource_type filter, pagination)

6. Run `sqlc generate` to produce Go code. Fix any query issues until generation succeeds.

Note: If `sqlc` is not installed, install it: `go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest`
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/services/api && sqlc generate && go build ./internal/db/...</automated>
  </verify>
  <done>sqlc generates without errors, all query Go files compile, types match migration schemas</done>
</task>

</tasks>

<verification>
- All migration files exist with correct SQL
- `sqlc generate` succeeds
- `go build ./internal/db/...` compiles
</verification>

<success_criteria>
- 3 migration files cover all required tables
- sqlc query files cover CRUD for users, payroll, webhooks, audit
- Generated Go code compiles
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-go-rest-api/06-02-SUMMARY.md`
</output>

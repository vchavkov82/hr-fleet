---
phase: 06-core-go-rest-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/go.mod
  - services/api/go.sum
  - services/api/internal/handler/response.go
  - services/api/internal/middleware/logging.go
  - services/api/internal/middleware/metrics.go
  - services/api/internal/middleware/tracing.go
  - services/api/internal/cache/cache.go
  - services/api/platform/odoo/client.go
  - services/api/platform/odoo/circuit_breaker_test.go
autonomous: true
requirements:
  - API-10
  - API-12
  - API-13

must_haves:
  truths:
    - "All new dependencies install and compile"
    - "Odoo calls are protected by circuit breaker and connection pool"
    - "L1 ristretto cache sits in front of Redis L2 cache"
    - "Structured JSON logging replaces log.Printf"
    - "Prometheus metrics endpoint exposes HTTP request metrics"
    - "Standardized error response format used across all handlers"
  artifacts:
    - path: "services/api/internal/handler/response.go"
      provides: "Shared JSON response helpers and error format"
      exports: ["RespondJSON", "RespondError", "ErrorResponse"]
    - path: "services/api/internal/middleware/logging.go"
      provides: "Zerolog HTTP request logging middleware"
    - path: "services/api/internal/middleware/metrics.go"
      provides: "Prometheus HTTP metrics middleware"
    - path: "services/api/platform/odoo/client.go"
      provides: "Circuit breaker + connection pool on Odoo client"
  key_links:
    - from: "services/api/platform/odoo/client.go"
      to: "gobreaker"
      via: "circuit breaker wrapping HTTP calls"
      pattern: "cb\\.Execute"
    - from: "services/api/internal/cache/cache.go"
      to: "ristretto"
      via: "L1 cache layer"
      pattern: "ristretto\\.NewCache"
---

<objective>
Install all new Go dependencies, add shared response helpers, observability middleware (zerolog, Prometheus, OTel tracing), extend Odoo client with circuit breaker + connection pooling, and add ristretto L1 cache.

Purpose: Foundation layer that all subsequent plans depend on — deps, shared patterns, resilience, observability.
Output: Compilable project with new deps, response helpers, logging/metrics middleware, resilient Odoo client, L1+L2 cache.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-go-rest-api/06-RESEARCH.md
@services/api/go.mod
@services/api/internal/cache/cache.go
@services/api/platform/odoo/client.go
@services/api/internal/handler/employee.go
@services/api/cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add response helpers and observability middleware</name>
  <files>services/api/go.mod, services/api/go.sum, services/api/internal/handler/response.go, services/api/internal/middleware/logging.go, services/api/internal/middleware/metrics.go, services/api/internal/middleware/tracing.go</files>
  <action>
1. Install all new deps from services/api/:
```bash
go get github.com/rs/zerolog github.com/sony/gobreaker github.com/hibiken/asynq github.com/swaggo/swag/cmd/swag github.com/swaggo/http-swagger github.com/dgraph-io/ristretto github.com/prometheus/client_golang/prometheus github.com/prometheus/client_golang/prometheus/promhttp go.opentelemetry.io/otel go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp golang.org/x/crypto/bcrypt github.com/go-playground/validator/v10 github.com/jackc/pgx/v5
```

2. Create `internal/handler/response.go`:
   - `ErrorResponse` struct: `{Error: {Code string, Message string, Details []FieldError}}`
   - `FieldError` struct: `{Field string, Message string}`
   - `RespondJSON(w, status, data)` — sets Content-Type, marshals JSON
   - `RespondError(w, status, code, message string, details ...FieldError)` — structured error
   - `RespondList(w, items, total, page, perPage)` — paginated list wrapper with `{data, meta: {total, page, per_page, total_pages}}`

3. Create `internal/middleware/logging.go`:
   - Use `rs/zerolog` with `hlog` (zerolog's HTTP handler helpers) or manual middleware
   - Log: method, path, status, duration_ms, request_id
   - Replace any existing log.Printf usage pattern

4. Create `internal/middleware/metrics.go`:
   - Prometheus middleware: `http_requests_total` counter (method, path, status), `http_request_duration_seconds` histogram
   - Register metrics, expose `/metrics` endpoint helper

5. Create `internal/middleware/tracing.go`:
   - OTel HTTP middleware using `otelhttp.NewHandler` wrapper
   - Span naming by route pattern
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/services/api && go build ./...</automated>
  </verify>
  <done>All deps installed, response helpers compile, middleware compiles, go build succeeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Odoo circuit breaker, connection pool, and ristretto L1 cache</name>
  <files>services/api/platform/odoo/client.go, services/api/platform/odoo/circuit_breaker_test.go, services/api/internal/cache/cache.go</files>
  <behavior>
    - Test: Circuit breaker opens after 5 consecutive failures, returns error without calling Odoo
    - Test: Circuit breaker allows requests again after timeout (half-open state)
    - Test: Connection pool limits concurrent Odoo requests to max 20 (configurable)
    - Test: L1 cache (ristretto) returns cached value without hitting Redis
    - Test: L1 miss falls through to Redis L2, then populates L1
  </behavior>
  <action>
1. Extend `platform/odoo/client.go`:
   - Add `*gobreaker.CircuitBreaker` field to Client struct
   - Add `sem chan struct{}` (buffered, cap=20) for connection pooling
   - Wrap all Odoo HTTP calls in `cb.Execute()` and semaphore acquire/release
   - Settings: MaxRequests=3, Interval=30s, Timeout=10s, ReadyToTrip after 5 consecutive failures

2. Extend `internal/cache/cache.go`:
   - Add ristretto L1 cache (`*ristretto.Cache[string, []byte]`)
   - Get: check L1 first, then L2 (Redis), populate L1 on L2 hit
   - Set: write to both L1 and L2
   - L1 TTL: 1 minute (short, in-process), L2 TTL: 5 minutes (existing pattern)
   - Stale cache: keep existing 30-min stale TTL pattern from Phase 3

3. Write tests in `platform/odoo/circuit_breaker_test.go`:
   - Mock HTTP transport to simulate failures
   - Verify circuit opens/closes correctly
   - Verify semaphore limits concurrency
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/services/api && go test ./platform/odoo/ -run TestCircuitBreaker -count=1 -v && go test ./internal/cache/ -count=1 -v</automated>
  </verify>
  <done>Circuit breaker opens after 5 failures, connection pool limits to 20, L1+L2 cache layering works, all tests pass</done>
</task>

</tasks>

<verification>
- `cd services/api && go build ./...` succeeds
- `cd services/api && go test ./... -short -count=1` passes
- Circuit breaker and cache tests pass
</verification>

<success_criteria>
- All new deps in go.mod and compile
- Structured error response format defined and usable
- Zerolog, Prometheus, OTel middleware ready to mount
- Odoo client has circuit breaker + connection pool
- L1 ristretto + L2 Redis cache layering works
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-go-rest-api/06-01-SUMMARY.md`
</output>

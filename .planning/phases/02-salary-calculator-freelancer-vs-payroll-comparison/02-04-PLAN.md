---
phase: 02-salary-calculator-freelancer-vs-payroll-comparison
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - www/tests/unit/freelancer-comparison.test.tsx
  - www/tests/e2e/calculators.spec.ts
autonomous: false
requirements: [CALC-05, CALC-07, CALC-10]

must_haves:
  truths:
    - "Component renders side-by-side columns with correct calculation results"
    - "Savings banner displays correct savings amount"
    - "Benefits table renders all 6 benefit rows"
    - "E2E test confirms page loads and calculator functions at /hr-tools/freelancer-comparison"
    - "All existing calculator tests still pass"
  artifacts:
    - path: "www/tests/unit/freelancer-comparison.test.tsx"
      provides: "Component render tests for FreelancerComparison"
      min_lines: 50
    - path: "www/tests/e2e/calculators.spec.ts"
      provides: "E2E tests for freelancer comparison page"
      contains: "freelancer"
  key_links:
    - from: "www/tests/unit/freelancer-comparison.test.tsx"
      to: "www/src/components/calculators/freelancer-comparison.tsx"
      via: "import and render"
      pattern: "import.*FreelancerComparison"
    - from: "www/tests/e2e/calculators.spec.ts"
      to: "/hr-tools/freelancer-comparison"
      via: "page.goto"
      pattern: "freelancer-comparison"
---

<objective>
Add component unit tests and E2E tests for the freelancer comparison calculator, then verify mobile responsiveness visually.

Purpose: Confirm the calculator works end-to-end in a real browser, that component renders correctly with all sections, and that mobile layout is usable.
Output: Passing unit and E2E tests, visual confirmation of mobile responsiveness.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-CONTEXT.md
@.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-02-SUMMARY.md
@.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-03-SUMMARY.md

@www/tests/e2e/calculators.spec.ts
@www/tests/unit/salary-calculator.test.tsx
@www/src/components/calculators/freelancer-comparison.tsx

<interfaces>
<!-- From existing calculators.spec.ts pattern -->
```typescript
import { test, expect } from '@playwright/test'
// Tests use: page.goto('/en/hr-tools/...'), page.locator(), expect(locator).toBeVisible()
```

<!-- From existing salary-calculator.test.tsx pattern -->
```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
// Component tests render with labels Record and assert on rendered output
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add component unit tests for FreelancerComparison</name>
  <files>www/tests/unit/freelancer-comparison.test.tsx</files>
  <action>
Create component render tests using @testing-library/react and vitest (same pattern as salary-calculator.test.tsx).

Build a mock labels Record with all required keys (can use simple English strings).

**Tests to write:**
1. Renders input section with amount field, born-after-1959 checkbox, toggles
2. Renders EOOD column with muted styling (check for class or text content)
3. Renders Employment column with highlighted styling
4. Renders savings banner with savings amount text
5. Renders benefits comparison table with all 6 rows (paid leave, sick pay, maternity, unemployment, mortgage, labor protection)
6. Renders CTA section with sign-up link
7. Renders editable overhead fields (accountant, bank, admin, registration)
8. Renders vacation days input in benefits section

Note: These are render tests (component mounts and shows correct structure), not interaction tests. Interaction tested via E2E.
  </action>
  <verify>
    <automated>cd www && bun run test -- freelancer-comparison.test</automated>
  </verify>
  <done>
- All component render tests pass
- Tests cover all major UI sections: input, columns, savings, benefits, CTA
- Mock labels used throughout (no hardcoded text assertions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add E2E tests for freelancer comparison page</name>
  <files>www/tests/e2e/calculators.spec.ts</files>
  <action>
Add a new `describe('Freelancer Comparison Calculator')` block to the existing calculators.spec.ts file. Do NOT modify existing tests.

**E2E tests to add:**

1. **Page loads and shows calculator:**
   - Navigate to `/en/hr-tools/freelancer-comparison`
   - Verify page title/heading is visible
   - Verify both EOOD and Employment columns are visible
   - Verify savings banner is visible

2. **Calculator responds to input:**
   - Fill amount input with 4000
   - Verify results update (savings banner shows a non-zero amount)
   - Verify EOOD net and Employment net values are displayed

3. **Benefits table is visible:**
   - Verify benefits section exists with at least 6 rows
   - Verify vacation days input is present

4. **SEO content present:**
   - Verify H1 heading exists
   - Verify at least 2 H2 sections below calculator

5. **Navigation from HR Tools:**
   - Navigate to `/en/hr-tools`
   - Click freelancer comparison card
   - Verify landed on freelancer-comparison page

6. **Bulgarian locale works:**
   - Navigate to `/bg/hr-tools/freelancer-comparison`
   - Verify page loads with Bulgarian text (check heading is not English)

Use resilient selectors (data attributes, roles, or text content) -- not brittle CSS class selectors.
  </action>
  <verify>
    <automated>cd www && npx playwright test tests/e2e/calculators.spec.ts --project=firefox</automated>
  </verify>
  <done>
- All new E2E tests pass in Firefox
- All existing calculator E2E tests still pass
- Tests verify page load, calculation, benefits table, SEO content, navigation, and locale
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete freelancer comparison calculator</name>
  <files>N/A</files>
  <action>
Human verifies the complete freelancer vs employment comparison calculator visually. All automated work is already complete from Tasks 1-2 and Plans 02-03.

What was built: Complete freelancer vs employment comparison calculator with side-by-side EOOD/Employment columns, savings messaging, benefits table, SEO content, and BG/EN localization.

How to verify:
1. Start dev server: `cd www && bun run dev`
2. Open http://localhost:3010/en/hr-tools/freelancer-comparison
3. Verify:
   - Side-by-side columns visible (EOOD muted gray, Employment green highlighted)
   - Enter 4000 BGN -- Employment column should show higher net income
   - Toggle monthly/annual -- annual shows larger savings number
   - Edit overhead fields (accountant, bank fees) -- EOOD result updates
   - Toggle illness/maternity -- EOOD insurance changes
   - Toggle VAT -- VAT breakdown appears in EOOD column
   - Scroll to benefits table -- 6 rows with check/cross marks
   - Edit vacation days -- monetized value updates
   - Savings banner shows green "Save X BGN" message
   - CTA button at bottom links to sign-up
4. Resize to mobile (375px width):
   - Columns stack vertically
   - Sticky CTA bar at bottom with savings amount
   - Content not cut off by sticky bar
5. Switch to Bulgarian: http://localhost:3010/bg/hr-tools/freelancer-comparison
   - All text in Bulgarian
6. Navigate from HR Tools page:
   - Go to http://localhost:3010/en/hr-tools
   - Verify new freelancer comparison card appears
   - Click it -- navigates to comparison page

Resume signal: Type "approved" or describe any visual/functional issues.
  </action>
  <verify>
    <automated>cd www && bun run test && npx playwright test tests/e2e/calculators.spec.ts --project=firefox</automated>
  </verify>
  <done>
- Human confirms visual layout is correct on desktop and mobile
- Human confirms Bulgarian translations read naturally
- Human confirms savings messaging is compelling and marketing-oriented
- All automated tests pass
  </done>
</task>

</tasks>

<verification>
cd www && bun run test
cd www && npx playwright test tests/e2e/calculators.spec.ts --project=firefox
cd www && bun run build
</verification>

<success_criteria>
- All unit tests pass (calculations + component)
- All E2E tests pass in Firefox
- Build succeeds with static generation for both locales
- Visual verification confirms mobile responsiveness and correct styling
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-04-SUMMARY.md`
</output>

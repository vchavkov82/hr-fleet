---
phase: 02-salary-calculator-freelancer-vs-payroll-comparison
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - www/src/app/[locale]/hr-tools/freelancer-comparison/page.tsx
  - www/messages/en/pages.json
  - www/messages/bg/pages.json
  - www/src/app/[locale]/hr-tools/page.tsx
autonomous: true
requirements: [CALC-08, CALC-09]

must_haves:
  truths:
    - "Page loads at /en/hr-tools/freelancer-comparison and /bg/hr-tools/freelancer-comparison"
    - "All UI text displays in Bulgarian when locale is bg"
    - "All UI text displays in English when locale is en"
    - "SEO content sections render above and below the calculator"
    - "Page has proper meta title and description for SEO"
    - "HR Tools main page has a new card linking to freelancer-comparison"
    - "Breadcrumb navigation shows HR Tools > Freelancer Comparison"
  artifacts:
    - path: "www/src/app/[locale]/hr-tools/freelancer-comparison/page.tsx"
      provides: "Server component wrapping FreelancerComparison client component"
      min_lines: 80
    - path: "www/messages/en/pages.json"
      provides: "English translations for freelancerComparison namespace"
      contains: "freelancerComparison"
    - path: "www/messages/bg/pages.json"
      provides: "Bulgarian translations for freelancerComparison namespace"
      contains: "freelancerComparison"
  key_links:
    - from: "www/src/app/[locale]/hr-tools/freelancer-comparison/page.tsx"
      to: "www/src/components/calculators/freelancer-comparison.tsx"
      via: "import FreelancerComparison"
      pattern: "import.*FreelancerComparison.*from.*freelancer-comparison"
    - from: "www/src/app/[locale]/hr-tools/page.tsx"
      to: "/hr-tools/freelancer-comparison"
      via: "CALCULATORS array entry"
      pattern: "freelancer-comparison"
---

<objective>
Create the server page component with SEO content, add BG/EN translations for all calculator labels, and wire the new page into HR Tools navigation.

Purpose: The server component handles SEO (meta, content sections), translations, and static generation. The HR Tools page needs a new card to make the calculator discoverable.
Output: Working page at `/hr-tools/freelancer-comparison` with full BG/EN localization and SEO content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-CONTEXT.md
@.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-RESEARCH.md

@www/src/app/[locale]/hr-tools/salary-calculator/page.tsx
@www/src/app/[locale]/hr-tools/page.tsx
@www/messages/en/pages.json
@www/messages/bg/pages.json

<interfaces>
<!-- Server page pattern from existing salary-calculator/page.tsx -->
```typescript
// Pattern to follow exactly:
import { setRequestLocale } from 'next-intl/server'
import { getTranslations } from 'next-intl/server'
import type { Metadata } from 'next'
import Link from 'next/link'
import { routing } from '@/i18n/routing'
import FreelancerComparison from '@/components/calculators/freelancer-comparison'

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }))
}

// FreelancerComparison accepts: { labels: Record<string, string> }
```

<!-- HR Tools page CALCULATORS array pattern -->
```typescript
const CALCULATORS = [
  { href: '/hr-tools/ai-assistant', icon: <svg.../>, labelKey: 'calculatorCards.aiAssistant' },
  { href: '/hr-tools/salary-calculator', icon: <svg.../>, labelKey: 'calculatorCards.salary' },
  { href: '/hr-tools/leave-calculator', icon: <svg.../>, labelKey: 'calculatorCards.leave' },
  { href: '/hr-tools/employment-cost-calculator', icon: <svg.../>, labelKey: 'calculatorCards.employmentCost' },
] as const
// Add new entry: { href: '/hr-tools/freelancer-comparison', icon: <svg.../>, labelKey: 'calculatorCards.freelancerComparison' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BG/EN translations for freelancer comparison</name>
  <files>www/messages/en/pages.json, www/messages/bg/pages.json</files>
  <action>
Add a `freelancerComparison` namespace under `hrTools` in both translation files. Group keys logically to avoid key explosion (per research pitfall #5).

**Key groups to add under `pages.hrTools.freelancerComparison`:**

```
metaTitle, metaDescription, heading, sectionLabel
introP1, introP2 (SEO intro paragraphs)

input.monthlyAmount, input.monthlyAmountHelp, input.bornAfter1960
input.illnessMaternity, input.illnessMaterityNote
input.vatRegistered, input.vatNote
input.monthly, input.annual

savings.banner, savings.perMonth, savings.perYear
savings.effectiveTaxRate, savings.eoodRate, savings.employmentRate
savings.totalWithVacation

eood.title, eood.revenue, eood.selfInsurance, eood.insuranceBase
eood.overhead, eood.accountantFee, eood.bankFees, eood.adminTime, eood.registrationAmortized
eood.totalOverhead, eood.taxableProfit, eood.corporateTax, eood.afterTaxProfit
eood.dividendTax, eood.netToOwner, eood.effectiveTaxRate
eood.vatBreakdown, eood.exclVat, eood.vatAmount, eood.totalInvoice, eood.vatNote

employment.title, employment.recommended, employment.grossSalary
employment.employeeDeductions, employment.incomeTax, employment.netSalary
employment.employerCost, employment.effectiveTaxRate

benefits.title
benefits.paidLeave, benefits.paidLeaveEood, benefits.paidLeaveEmployment
benefits.sickPay, benefits.sickPayEood, benefits.sickPayEmployment
benefits.maternity, benefits.maternityEood, benefits.maternityEmployment
benefits.unemployment, benefits.unemploymentEood, benefits.unemploymentEmployment
benefits.mortgage, benefits.mortgageEood, benefits.mortgageEmployment
benefits.laborProtection, benefits.laborProtectionEood, benefits.laborProtectionEmployment
benefits.vacationDays, benefits.vacationDaysMin, benefits.vacationValue

disclaimer
cta.title, cta.description, cta.button
mobileCta.savings, mobileCta.button

seo.howItWorksTitle, seo.howItWorksP1, seo.howItWorksP2
seo.eoodVsEmploymentTitle, seo.eoodVsEmploymentP1, seo.eoodVsEmploymentP2
seo.hiddenCostsTitle, seo.hiddenCostsP1
```

**English translations:** Professional, clear, marketing-oriented. SEO content targets "freelancer vs employment Bulgaria", "EOOD vs payroll".

**Bulgarian translations:** Natural Bulgarian business language. SEO content targets "ЕООД vs трудов договор", "фрийлансър срещу трудов договор", "самоосигуряващо се лице срещу назначение".

Also add `calculatorCards.freelancerComparison.title` and `calculatorCards.freelancerComparison.description` for the HR Tools main page card.
  </action>
  <verify>
    <automated>cd www && npx tsc --noEmit</automated>
  </verify>
  <done>
- Both en/pages.json and bg/pages.json have freelancerComparison namespace with all keys
- Both files are valid JSON
- Bulgarian text is natural, not machine-translated
- SEO content targets the correct keywords in each language
- calculatorCards.freelancerComparison keys exist for HR Tools page
  </done>
</task>

<task type="auto">
  <name>Task 2: Create server page and wire into HR Tools navigation</name>
  <files>www/src/app/[locale]/hr-tools/freelancer-comparison/page.tsx, www/src/app/[locale]/hr-tools/page.tsx</files>
  <action>
**Create `freelancer-comparison/page.tsx`** following the exact pattern from salary-calculator/page.tsx:

1. `generateStaticParams()` using `routing.locales`
2. `generateMetadata()` using `getTranslations('pages.hrTools.freelancerComparison')` for metaTitle/metaDescription
3. Default export server component that:
   - Calls `setRequestLocale(locale)` and `getTranslations('pages.hrTools.freelancerComparison')`
   - Builds labels Record mapping ALL translation keys to `t()` calls
   - Renders: breadcrumb nav -> SEO content above (heading, intro paragraphs) -> `<FreelancerComparison labels={labels} />` -> SEO content below (3 sections: how it works, EOOD vs employment explanation, hidden costs) -> CTA section

**SEO content structure above calculator:**
- Section label badge
- H1 heading (primary SEO target keyword)
- Two intro paragraphs explaining the comparison tool

**SEO content structure below calculator:**
- H2: How the comparison works (explain calculation methodology)
- H2: EOOD vs Employment in Bulgaria (explain the tax difference for SEO)
- H2: Hidden costs of running an EOOD (explain overhead for SEO)

**Breadcrumb:** `HR Tools / Freelancer Comparison` (linked HR Tools, current page plain text)

**Update `hr-tools/page.tsx`:**
- Add new entry to CALCULATORS array: `{ href: '/hr-tools/freelancer-comparison', icon: <svg ... />, labelKey: 'calculatorCards.freelancerComparison' }`
- Use a comparison/scale icon SVG (two balanced items suggesting comparison)
- Change grid from `md:grid-cols-3` to `md:grid-cols-3 lg:grid-cols-5` or keep 3-col and let it wrap, whichever looks better with 5 items
  </action>
  <verify>
    <automated>cd www && npx tsc --noEmit && bun run test</automated>
  </verify>
  <done>
- /en/hr-tools/freelancer-comparison renders the server page with SEO content and client calculator
- /bg/hr-tools/freelancer-comparison renders with Bulgarian translations
- Meta title and description are set for SEO
- Breadcrumb navigation works
- HR Tools main page shows new freelancer comparison card
- All existing tests still pass
  </done>
</task>

</tasks>

<verification>
cd www && npx tsc --noEmit
cd www && bun run test
cd www && bun run build (verify static generation works for new route)
</verification>

<success_criteria>
- Page builds successfully with `bun run build` (static generation for both locales)
- All text on the page comes from translation files (no hardcoded strings)
- Bulgarian translations read naturally (not machine-translated)
- SEO content sections render above and below the calculator
- HR Tools page has 5 calculator cards including freelancer comparison
- Breadcrumb navigation consistent with existing calculator pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-salary-calculator-freelancer-vs-payroll-comparison/02-03-SUMMARY.md`
</output>

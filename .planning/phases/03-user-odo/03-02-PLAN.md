---
phase: 03-user-odo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/platform/odoo/client.go
  - backend/platform/odoo/auth.go
  - backend/platform/odoo/employee.go
  - backend/platform/odoo/types.go
  - backend/platform/odoo/client_test.go
autonomous: true
requirements:
  - EMP-01
  - EMP-02

must_haves:
  truths:
    - "Go client can authenticate with Odoo via JSON-RPC"
    - "Go client can search_read hr.employee records with domain filters"
    - "Go client can create hr.employee records"
    - "Go client handles Odoo session expiry by re-authenticating"
  artifacts:
    - path: "backend/platform/odoo/client.go"
      provides: "JSON-RPC HTTP client with session management"
      exports: ["Client", "NewClient"]
      min_lines: 80
    - path: "backend/platform/odoo/auth.go"
      provides: "Odoo authentication and session refresh"
      exports: ["Authenticate"]
    - path: "backend/platform/odoo/employee.go"
      provides: "hr.employee CRUD operations"
      exports: ["ListEmployees", "GetEmployee", "CreateEmployee", "UpdateEmployee"]
    - path: "backend/platform/odoo/types.go"
      provides: "Request/response types for JSON-RPC and employee data"
      exports: ["JSONRPCRequest", "JSONRPCResponse", "Employee", "EmployeeCreateRequest"]
    - path: "backend/platform/odoo/client_test.go"
      provides: "Unit tests with HTTP mock server"
      min_lines: 100
  key_links:
    - from: "backend/platform/odoo/client.go"
      to: "Odoo /jsonrpc endpoint"
      via: "HTTP POST"
      pattern: "jsonrpc"
    - from: "backend/platform/odoo/employee.go"
      to: "backend/platform/odoo/client.go"
      via: "method calls"
      pattern: "c\\.Call|c\\.SearchRead|c\\.Create"
---

<objective>
Build the Go JSON-RPC client for Odoo 18 communication. Covers authentication, session management, and hr.employee CRUD operations.

Purpose: This is the translation layer between Go and Odoo. All HR data flows through this client. Building it in isolation with mock tests allows parallel development with infrastructure (Plan 01).
Output: Tested Odoo JSON-RPC client package with employee CRUD methods.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-user-odo/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement JSON-RPC client with types and authentication</name>
  <files>backend/platform/odoo/types.go, backend/platform/odoo/client.go, backend/platform/odoo/auth.go, backend/platform/odoo/client_test.go</files>
  <behavior>
    - Test 1: Client.Authenticate returns uid on valid credentials (mock /jsonrpc response)
    - Test 2: Client.Authenticate returns error on invalid credentials
    - Test 3: Client.Call sends properly formatted JSON-RPC 2.0 request
    - Test 4: Client.Call parses error response and returns Go error
    - Test 5: Client re-authenticates on session expiry (401/AccessDenied)
  </behavior>
  <action>
    Create backend/platform/odoo/types.go:
    - JSONRPCRequest: jsonrpc string, method string, params interface{}, id int
    - JSONRPCResponse: jsonrpc string, id int, result json.RawMessage, error *RPCError
    - RPCError: code int, message string, data interface{}
    - Employee struct: ID int64, Name string, WorkEmail string, JobTitle string, DepartmentID [2]interface{} (Odoo many2one format), JobID [2]interface{}, ParentID [2]interface{}, WorkPhone string, EmployeeType string, Active bool, CreateDate string, WriteDate string
    - EmployeeCreateRequest: Name string, WorkEmail string, JobTitle string, DepartmentID int64, EmployeeType string
    - Define employeeFields slice with all field names to request from Odoo

    Create backend/platform/odoo/client.go:
    - Client struct: baseURL, db, uid int64, username, password string, httpClient *http.Client, sessionID string, mu sync.Mutex
    - NewClient(baseURL, db, username, password) *Client
    - Call(service, method string, args []interface{}) (json.RawMessage, error): Builds JSONRPCRequest, POSTs to /jsonrpc, handles session cookie, parses response
    - SearchRead(model string, domain []interface{}, fields []string, limit, offset int) ([]map[string]interface{}, error): Calls execute_kw with search_read
    - Create(model string, vals map[string]interface{}) (int64, error): Calls execute_kw with create
    - Write(model string, id int64, vals map[string]interface{}) error: Calls execute_kw with write

    Create backend/platform/odoo/auth.go:
    - Authenticate() error: Calls common service login, stores uid and session cookie
    - EnsureAuthenticated() error: Checks if authenticated, re-authenticates if needed
    - Handle 401/AccessDenied by re-authenticating once and retrying the call

    Tests use httptest.NewServer to mock Odoo /jsonrpc responses. No real Odoo needed.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/backend && go test ./platform/odoo/ -v -count=1</automated>
  </verify>
  <done>JSON-RPC client authenticates, sends properly formatted requests, handles errors and session expiry. All tests pass with mock HTTP server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hr.employee CRUD operations</name>
  <files>backend/platform/odoo/employee.go, backend/platform/odoo/client_test.go</files>
  <behavior>
    - Test 1: ListEmployees returns slice of Employee structs from search_read response
    - Test 2: ListEmployees applies domain filters (department, active status)
    - Test 3: ListEmployees respects limit and offset for pagination
    - Test 4: GetEmployee returns single Employee by ID
    - Test 5: CreateEmployee sends correct fields and returns new ID
    - Test 6: UpdateEmployee sends write call with changed fields only
  </behavior>
  <action>
    Create backend/platform/odoo/employee.go:
    - ListEmployees(ctx, domain []interface{}, limit, offset int) ([]Employee, int, error):
      - Calls SearchRead on "hr.employee" with employeeFields
      - Also calls search_count for total count (pagination)
      - Parses many2one fields (department_id, job_id, parent_id) into {ID, Name} format
      - Returns parsed Employee slice and total count

    - GetEmployee(ctx, id int64) (*Employee, error):
      - Calls Read on "hr.employee" with single ID
      - Returns parsed Employee

    - CreateEmployee(ctx, req EmployeeCreateRequest) (int64, error):
      - Converts EmployeeCreateRequest to map[string]interface{} with Odoo field names
      - Calls Create on "hr.employee"
      - Returns new record ID

    - UpdateEmployee(ctx, id int64, vals map[string]interface{}) error:
      - Calls Write on "hr.employee" with ID and values
      - Only sends provided fields (partial update)

    Add tests to client_test.go with mock responses matching Odoo 18 JSON-RPC format.
    Use the employeeFields slice from types.go to ensure consistency.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/backend && go test ./platform/odoo/ -v -count=1 -run "Employee"</automated>
  </verify>
  <done>All employee CRUD methods work against mock Odoo responses. Field mapping from Odoo many2one format to Go structs is correct.</done>
</task>

</tasks>

<verification>
- `cd backend && go test ./platform/odoo/ -v` passes all tests
- `cd backend && go build ./...` compiles entire project
</verification>

<success_criteria>
Odoo JSON-RPC client package is complete with authentication, session management, and hr.employee CRUD. All tested with HTTP mock server. Ready to be consumed by service layer (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-odo/03-02-SUMMARY.md`
</output>

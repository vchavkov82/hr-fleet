---
phase: 03-user-odo
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - www/src/app/[locale]/dashboard/layout.tsx
  - www/src/app/[locale]/dashboard/page.tsx
  - www/src/app/[locale]/dashboard/employees/page.tsx
  - www/src/app/[locale]/dashboard/employees/[id]/page.tsx
  - www/src/components/dashboard/sidebar-nav.tsx
  - www/src/components/dashboard/metric-card.tsx
  - www/src/components/dashboard/employee-table.tsx
  - www/src/components/dashboard/employee-form.tsx
  - www/src/lib/api.ts
  - www/src/lib/types/employee.ts
  - www/package.json
  - www/messages/bg/dashboard.json
  - www/messages/en/dashboard.json
autonomous: true
requirements:
  - EMP-01
  - EMP-02

must_haves:
  truths:
    - "Dashboard layout has sidebar navigation and main content area"
    - "Employee directory page shows table with name, email, department, position, start date, status columns"
    - "Employee table supports sorting by clicking column headers"
    - "Employee table supports filtering by search text and department"
    - "Employee table has pagination controls"
    - "Employee create form captures name, email, role, start date, contract type"
    - "Employee detail page shows full profile with edit capability"
    - "All dashboard UI text is localized in BG and EN"
  artifacts:
    - path: "www/src/app/[locale]/dashboard/layout.tsx"
      provides: "Authenticated dashboard layout with sidebar"
      min_lines: 30
    - path: "www/src/app/[locale]/dashboard/employees/page.tsx"
      provides: "Employee directory page"
      min_lines: 20
    - path: "www/src/components/dashboard/employee-table.tsx"
      provides: "TanStack Table for employee directory"
      min_lines: 100
    - path: "www/src/components/dashboard/employee-form.tsx"
      provides: "Employee create/edit form with validation"
      min_lines: 80
    - path: "www/src/lib/api.ts"
      provides: "API client for Go backend"
      exports: ["fetchEmployees", "fetchEmployee", "createEmployee", "updateEmployee"]
    - path: "www/src/lib/types/employee.ts"
      provides: "TypeScript types for employee data"
      exports: ["Employee", "EmployeeCreateInput", "EmployeeListResponse"]
  key_links:
    - from: "www/src/components/dashboard/employee-table.tsx"
      to: "www/src/lib/api.ts"
      via: "fetch call"
      pattern: "fetchEmployees"
    - from: "www/src/components/dashboard/employee-form.tsx"
      to: "www/src/lib/api.ts"
      via: "submit handler"
      pattern: "createEmployee|updateEmployee"
    - from: "www/src/lib/api.ts"
      to: "http://localhost:8080/api/v1"
      via: "fetch"
      pattern: "api/v1/employees"
---

<objective>
Build the Next.js dashboard UI: authenticated layout with sidebar, employee directory table (TanStack Table), employee create/edit forms, API client, and BG/EN translations.

Purpose: The user-facing interface for employee management. Uses mock data initially, wired to Go backend in Plan 05.
Output: Complete dashboard UI with employee directory, forms, and localized labels.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-user-odo/03-RESEARCH.md
@www/src/middleware.ts
@www/src/app/[locale]/hr-tools/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout, sidebar, types, and API client</name>
  <files>www/src/app/[locale]/dashboard/layout.tsx, www/src/components/dashboard/sidebar-nav.tsx, www/src/components/dashboard/metric-card.tsx, www/src/lib/types/employee.ts, www/src/lib/api.ts, www/messages/bg/dashboard.json, www/messages/en/dashboard.json</files>
  <action>
    1. **www/src/lib/types/employee.ts**: Define TypeScript types:
       ```typescript
       export interface Employee {
         id: number; name: string; workEmail: string; jobTitle: string;
         department: { id: number; name: string };
         job: { id: number; name: string };
         manager: { id: number; name: string } | null;
         workPhone: string; employeeType: string;
         active: boolean; createDate: string; writeDate: string;
       }
       export interface EmployeeCreateInput {
         name: string; workEmail: string; jobTitle: string;
         departmentId: number; employeeType: string;
       }
       export interface EmployeeListResponse {
         data: Employee[];
         pagination: { page: number; perPage: number; total: number; totalPages: number };
       }
       ```

    2. **www/src/lib/api.ts**: API client functions:
       - API_BASE from env var NEXT_PUBLIC_API_URL || 'http://localhost:8080'
       - fetchEmployees(token, params?): GET /api/v1/employees with query params
       - fetchEmployee(token, id): GET /api/v1/employees/{id}
       - createEmployee(token, data): POST /api/v1/employees
       - updateEmployee(token, id, data): PUT /api/v1/employees/{id}
       - All functions include Authorization: Bearer header
       - Error handling: throw typed errors with status code and message

    3. **www/src/app/[locale]/dashboard/layout.tsx**: Server component:
       - Sidebar on left (240px, collapsible on mobile), main content on right
       - Use existing corporate blue palette from the marketing site
       - Import SidebarNav component
       - Wrap children in main content area with padding

    4. **www/src/components/dashboard/sidebar-nav.tsx**: Client component:
       - Navigation links: Dashboard (overview), Employees, (future: Leave, Recruitment)
       - Active state highlighting using usePathname()
       - Company name/logo at top
       - Responsive: full sidebar on lg+, hamburger menu on mobile
       - Use Tailwind classes consistent with existing design (corporate blue #1e3a8a)

    5. **www/src/components/dashboard/metric-card.tsx**: Server component:
       - Props: title, value, icon, trend (optional)
       - Card with shadow, icon, large value display, optional trend indicator
       - Reusable for dashboard overview metrics

    6. **Translation files**: Create www/messages/bg/dashboard.json and www/messages/en/dashboard.json:
       - sidebar: { overview, employees, leave, recruitment, settings }
       - employees: { title, addNew, search, department, position, startDate, status, active, inactive, allDepartments, noResults, form.name, form.email, form.jobTitle, form.department, form.employeeType, form.save, form.cancel, types.employee, types.contractor, types.trainee }
       - overview: { totalEmployees, pendingLeave, recentHires, upcomingBirthdays }

    Update www/src/middleware.ts to add 'dashboard' namespace to next-intl if needed for message loading.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/www && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "PASS"</automated>
  </verify>
  <done>Dashboard layout with sidebar navigation renders. API client typed. Translation files for BG/EN exist. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build employee directory table and create/edit form</name>
  <files>www/src/app/[locale]/dashboard/page.tsx, www/src/app/[locale]/dashboard/employees/page.tsx, www/src/app/[locale]/dashboard/employees/[id]/page.tsx, www/src/components/dashboard/employee-table.tsx, www/src/components/dashboard/employee-form.tsx, www/package.json</files>
  <action>
    Install TanStack Table: `cd www && pnpm add @tanstack/react-table`

    1. **www/src/components/dashboard/employee-table.tsx**: Client component using TanStack Table:
       - Columns: Name (sortable), Email, Department (sortable), Position, Start Date (sortable), Status (active/inactive badge)
       - Features: column sorting (getSortedRowModel), global text filter (getFilteredRowModel), pagination (getPaginationRowModel, 20 per page)
       - Search input at top with department dropdown filter
       - "Add Employee" button linking to create form/modal
       - Pagination controls at bottom (page X of Y, prev/next)
       - Empty state when no results
       - Loading skeleton while data is fetching
       - Use Tailwind: border-collapse table, hover:bg-gray-50 rows, blue action buttons
       - All labels from useTranslations('dashboard.employees')

    2. **www/src/components/dashboard/employee-form.tsx**: Client component:
       - Props: employee? (for edit mode), onSubmit, onCancel
       - Fields: name (text, required), workEmail (email, required), jobTitle (text), departmentId (select), employeeType (select: employee/contractor/trainee), startDate (date)
       - Client-side validation: name min 2 chars, email format, required fields
       - Use controlled inputs (useState) -- keep it simple, no form library needed for 6 fields
       - Submit button with loading state
       - All labels from translations

    3. **www/src/app/[locale]/dashboard/page.tsx**: Dashboard overview page:
       - Grid of MetricCard components: Total Employees, Pending Leave Requests, Recent Hires (last 30 days), Upcoming Birthdays
       - Use placeholder/mock values for now (will be wired to API in Plan 05)
       - Quick links to Employees, Leave, etc.
       - Server component with translations

    4. **www/src/app/[locale]/dashboard/employees/page.tsx**: Employee directory page:
       - Server component wrapper that renders EmployeeTable
       - For now, pass mock employee data (5-10 sample employees) so the table is visually testable
       - Page title and breadcrumb

    5. **www/src/app/[locale]/dashboard/employees/[id]/page.tsx**: Employee detail page:
       - Shows employee profile: name, email, department, position, phone, type, status
       - Edit button that toggles EmployeeForm in edit mode
       - Back link to directory
       - Use mock data for now

    Do NOT import react-hook-form or zod -- simple controlled inputs are sufficient for 6 form fields.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/www && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "PASS"</automated>
  </verify>
  <done>Employee directory table renders with sorting, filtering, pagination. Employee form captures all required fields with validation. Dashboard overview shows metric cards. All pages use translations.</done>
</task>

</tasks>

<verification>
- `cd www && npx tsc --noEmit` passes
- Dashboard pages exist at /dashboard, /dashboard/employees, /dashboard/employees/[id]
- Employee table uses TanStack Table with sorting and filtering
- BG/EN translation files contain all dashboard labels
</verification>

<success_criteria>
Complete dashboard UI: layout with sidebar, employee directory with sortable/filterable table, employee create/edit form, overview with metric cards. All localized. Renders with mock data, ready for API wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-odo/03-04-SUMMARY.md`
</output>

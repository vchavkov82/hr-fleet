---
phase: 03-user-odo
plan: 05
type: execute
wave: 3
depends_on:
  - 03-03
  - 03-04
files_modified:
  - www/src/app/[locale]/dashboard/employees/page.tsx
  - www/src/app/[locale]/dashboard/employees/[id]/page.tsx
  - www/src/app/[locale]/dashboard/page.tsx
  - www/src/components/dashboard/employee-table.tsx
  - www/src/components/dashboard/employee-form.tsx
  - www/src/middleware.ts
  - www/src/lib/api.ts
  - www/tests/unit/employee-table.test.tsx
  - www/tests/unit/employee-form.test.tsx
  - www/tests/e2e/dashboard.spec.ts
  - deploy/podman-compose.yml
  - deploy/Caddyfile
autonomous: false
requirements:
  - EMP-01
  - EMP-02

must_haves:
  truths:
    - "Frontend fetches real employee data from Go backend API"
    - "Creating an employee through the form persists to Odoo via Go API"
    - "Employee directory loads with real data from Odoo"
    - "Caddy proxies /api/v1 requests to Go backend"
    - "Dashboard is only accessible with valid authentication"
    - "E2E test can navigate to dashboard and see employee table"
  artifacts:
    - path: "www/tests/unit/employee-table.test.tsx"
      provides: "Unit tests for employee table component"
      min_lines: 50
    - path: "www/tests/unit/employee-form.test.tsx"
      provides: "Unit tests for employee form component"
      min_lines: 50
    - path: "www/tests/e2e/dashboard.spec.ts"
      provides: "E2E tests for dashboard employee flow"
      min_lines: 40
    - path: "deploy/Caddyfile"
      provides: "Reverse proxy config including Go backend"
      contains: "api/v1"
  key_links:
    - from: "www/src/components/dashboard/employee-table.tsx"
      to: "http://localhost:8080/api/v1/employees"
      via: "www/src/lib/api.ts fetchEmployees"
      pattern: "fetchEmployees"
    - from: "www/src/components/dashboard/employee-form.tsx"
      to: "http://localhost:8080/api/v1/employees"
      via: "www/src/lib/api.ts createEmployee"
      pattern: "createEmployee"
    - from: "deploy/Caddyfile"
      to: "hr-api:8080"
      via: "reverse_proxy"
      pattern: "reverse_proxy.*hr-api"
---

<objective>
Wire the frontend to the Go backend, add Caddy proxy configuration, update auth middleware, write unit and E2E tests, and verify the full stack works end-to-end.

Purpose: Connect all the layers built in Plans 01-04 into a working system. Prove that employee CRUD flows from browser through Go to Odoo and back.
Output: Fully integrated dashboard with real API calls, proxy config, and comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-user-odo/03-RESEARCH.md
@.planning/phases/03-user-odo/03-03-SUMMARY.md
@.planning/phases/03-user-odo/03-04-SUMMARY.md
@deploy/Caddyfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire frontend to Go API, update Caddy proxy, add auth middleware</name>
  <files>www/src/app/[locale]/dashboard/employees/page.tsx, www/src/app/[locale]/dashboard/employees/[id]/page.tsx, www/src/app/[locale]/dashboard/page.tsx, www/src/components/dashboard/employee-table.tsx, www/src/components/dashboard/employee-form.tsx, www/src/middleware.ts, www/src/lib/api.ts, deploy/Caddyfile, deploy/podman-compose.yml</files>
  <action>
    1. **Update www/src/middleware.ts**: Add JWT auth check for /dashboard routes:
       - Check for auth cookie/token on /dashboard/* paths
       - If no token, redirect to /auth/login with returnUrl query param
       - Keep existing next-intl middleware for locale routing
       - For MVP: use a simple cookie check (detailed JWT verification can be enhanced later)

    2. **Update employee-table.tsx**: Replace mock data with real API calls:
       - Use useEffect + useState to fetch from API on mount and when filters change
       - Pass auth token from cookie/context to fetchEmployees
       - Handle loading state (show skeleton)
       - Handle error state (show error message with retry)
       - Handle empty state (no employees yet)
       - Debounce search input (300ms)

    3. **Update employee-form.tsx**: Wire submit to API:
       - On submit, call createEmployee or updateEmployee via api.ts
       - Show loading state on button during submission
       - Show success toast/notification on success
       - Show error message on failure
       - Redirect to employee directory after successful create

    4. **Update dashboard page.tsx**: Wire overview metrics to API:
       - Fetch employee count for "Total Employees" metric card
       - Other metrics can remain placeholder (leave, birthdays -- future phases)

    5. **Update employee detail page**: Wire to fetchEmployee API call

    6. **Update deploy/Caddyfile**: Add reverse proxy for Go backend:
       - Route /api/v1/* to hr-api:8080
       - Set CORS headers for development
       - Keep existing routes for www, blog, docs

    7. **Update deploy/podman-compose.yml**: Add hr-api service to Caddy's depends_on

    8. **Update www/src/lib/api.ts**: Ensure API base URL works through Caddy proxy in production and directly in development.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/www && npx tsc --noEmit --pretty 2>&1 | head -30 && grep -q "api/v1" /home/vchavkov/src/hr/deploy/Caddyfile && echo "PASS"</automated>
  </verify>
  <done>Frontend fetches real data from Go API. Caddy proxies API requests. Auth middleware protects dashboard. Mock data removed from employee pages.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for employee components and E2E test for dashboard flow</name>
  <files>www/tests/unit/employee-table.test.tsx, www/tests/unit/employee-form.test.tsx, www/tests/e2e/dashboard.spec.ts</files>
  <action>
    1. **www/tests/unit/employee-table.test.tsx** (Vitest + React Testing Library):
       - Test: renders table with employee data
       - Test: sorts by name column when header clicked
       - Test: filters by search text
       - Test: shows empty state when no employees
       - Test: shows loading skeleton while fetching
       - Test: pagination controls display correct page info
       - Mock fetch/API calls using vi.mock

    2. **www/tests/unit/employee-form.test.tsx** (Vitest + React Testing Library):
       - Test: renders all form fields (name, email, jobTitle, department, employeeType)
       - Test: shows validation error when name is empty
       - Test: shows validation error when email is invalid
       - Test: calls onSubmit with form data when valid
       - Test: shows loading state during submission
       - Test: pre-fills fields in edit mode
       - Mock translations using next-intl test utilities

    3. **www/tests/e2e/dashboard.spec.ts** (Playwright):
       - Test: navigating to /dashboard redirects to login if not authenticated
       - Test: authenticated user can see dashboard overview
       - Test: authenticated user can navigate to employee directory
       - Test: employee table renders with data (mock API or test fixtures)
       - Use Firefox browser per project convention (Chromium crashes on openSUSE)
       - Note: Full E2E with real Odoo requires running infrastructure; mark those tests as requiring setup
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr/www && bun run test -- employee-table employee-form 2>&1 | tail -20 && echo "PASS"</automated>
  </verify>
  <done>Unit tests pass for employee table (sort, filter, pagination, empty state) and form (validation, submit, edit mode). E2E test file exists for dashboard flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full Odoo HR integration</name>
  <action>
    Human verifies the full stack works end-to-end. What was built:
    Full Odoo HR backend integration -- Go API proxy, employee directory with TanStack Table, create/edit forms, Redis caching, JWT auth, Caddy proxy config.

    Steps to verify:
    1. Start infrastructure: `cd deploy && podman-compose up -d`
    2. Initialize Odoo modules: `bash deploy/odoo-init.sh`
    3. Start Go backend: `cd backend && go run ./cmd/server/`
    4. Start Next.js: `cd www && bun run dev`
    5. Navigate to http://localhost:3010/en/dashboard
    6. Verify: Dashboard overview shows metric cards
    7. Click "Employees" in sidebar
    8. Verify: Employee table renders with columns (name, email, department, position, start date, status)
    9. Click "Add Employee" and fill the form
    10. Verify: Employee appears in the table after creation
    11. Click an employee name to view detail page
    12. Verify: Profile displays all fields
    13. Test sorting: click column headers
    14. Test filtering: type in search box
    15. Switch to Bulgarian: verify all labels are translated
  </action>
  <verify>User confirms: type "approved" or describe issues</verify>
  <done>User has approved the full integration: dashboard renders, employee CRUD works, sorting/filtering functional, BG/EN translations correct.</done>
</task>

</tasks>

<verification>
- `cd www && bun run test` passes all unit tests (existing + new employee tests)
- `cd backend && go test ./...` passes all Go tests
- `cd www && npx tsc --noEmit` passes
- Dashboard accessible at /dashboard with employee directory
- Employee CRUD works end-to-end (browser -> Go -> Odoo)
</verification>

<success_criteria>
Full integration verified: employee directory loads real data from Odoo through Go API, employee creation persists to Odoo, table sorting/filtering works, forms validate, tests pass, user confirms visual quality.
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-odo/03-05-SUMMARY.md`
</output>

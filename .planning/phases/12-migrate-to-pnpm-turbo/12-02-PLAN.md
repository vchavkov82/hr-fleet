---
phase: 12-migrate-to-pnpm-turbo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - DEVELOPER.md
autonomous: true
requirements:
  - CLEANUP-02
must_haves:
  truths:
    - "Documentation only references pnpm for package management"
    - "Developer onboarding docs use pnpm commands"
  artifacts:
    - path: "CLAUDE.md"
      provides: "AI assistant instructions with pnpm commands"
      contains: "pnpm"
    - path: "DEVELOPER.md"
      provides: "Developer setup guide with pnpm commands"
      contains: "pnpm"
  key_links:
    - from: "DEVELOPER.md"
      to: "pnpm"
      via: "setup and command instructions"
      pattern: "pnpm (install|dev|build|test)"
---

<objective>
Update all documentation to reference pnpm instead of bun.

Purpose: Ensure developer docs and AI instructions match the actual toolchain.
Output: Documentation that consistently references pnpm.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-migrate-to-pnpm-turbo/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and DEVELOPER.md</name>
  <files>CLAUDE.md, DEVELOPER.md</files>
  <action>
1. Read both files fully first.
2. In `CLAUDE.md` (lines ~54-56): Replace `bun dev`, `bun build`, `bun preview` commands with `pnpm dev`, `pnpm build`, `pnpm preview`. Search entire file for any other bun references.
3. In `DEVELOPER.md`: Replace ALL bun references (lines ~44, ~60, ~284-286, ~382, ~453, ~461, ~486 per research). Specifically:
   - Replace `bun install` with `pnpm install`
   - Replace `bun dev` with `pnpm dev`
   - Replace `bun run` with `pnpm run`
   - Replace `bun add` with `pnpm add`
   - Replace `bunx` with `pnpm dlx` or `npx`
   - Update any "Prerequisites" sections to mention pnpm instead of bun
   - Keep the same structure and formatting, just swap the tool references
4. Run grep to confirm zero remaining bun references in both files.
  </action>
  <verify>
    <automated>grep -n "\bbun\b" CLAUDE.md DEVELOPER.md 2>&1 | grep -c "bun" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL: bun references remain in docs"</automated>
  </verify>
  <done>Both CLAUDE.md and DEVELOPER.md reference only pnpm. No bun mentions remain in documentation.</done>
</task>

</tasks>

<verification>
```bash
grep -rn "\bbun\b" CLAUDE.md DEVELOPER.md
# Should return empty
```
</verification>

<success_criteria>
- CLAUDE.md uses pnpm commands exclusively
- DEVELOPER.md uses pnpm commands exclusively
- Zero bun references in any documentation file
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-to-pnpm-turbo/12-02-SUMMARY.md`
</output>

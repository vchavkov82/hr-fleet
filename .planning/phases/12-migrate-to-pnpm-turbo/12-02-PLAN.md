---
phase: 12-migrate-to-pnpm-turbo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - DEVELOPER.md
  - apps/blog/CLAUDE.md
  - apps/web/TESTING.md
  - .docs/AGENT_SETUP.md
  - .docs/QUICK_START.md
  - apps/web/scripts/dev-apps.sh
  - .claude/agents/admin-panel.md
  - .claude/agents/frontend-perf.md
  - .claude/agents/nextjs-frontend.md
autonomous: true
requirements:
  - CLEANUP-02
must_haves:
  truths:
    - "Documentation only references pnpm for package management"
    - "Developer onboarding docs use pnpm commands"
    - "App-specific docs use pnpm commands"
    - "Testing docs use pnpm commands"
    - "Agent configuration files use pnpm commands"
  artifacts:
    - path: "CLAUDE.md"
      provides: "AI assistant instructions with pnpm commands"
      contains: "pnpm"
    - path: "DEVELOPER.md"
      provides: "Developer setup guide with pnpm commands"
      contains: "pnpm"
    - path: "apps/blog/CLAUDE.md"
      provides: "Blog app instructions with pnpm commands"
      contains: "pnpm"
    - path: "apps/web/TESTING.md"
      provides: "Testing guide with pnpm commands"
      contains: "pnpm"
    - path: ".docs/AGENT_SETUP.md"
      provides: "Agent setup with pnpm commands"
      contains: "pnpm"
    - path: ".docs/QUICK_START.md"
      provides: "Quick start with pnpm commands"
      contains: "pnpm"
    - path: ".claude/agents/admin-panel.md"
      provides: "Admin panel agent config with pnpm commands"
      contains: "pnpm"
    - path: ".claude/agents/frontend-perf.md"
      provides: "Frontend perf agent config with pnpm commands"
      contains: "pnpm"
    - path: ".claude/agents/nextjs-frontend.md"
      provides: "Next.js frontend agent config with pnpm commands"
      contains: "pnpm"
  key_links:
    - from: "DEVELOPER.md"
      to: "pnpm"
      via: "setup and command instructions"
      pattern: "pnpm (install|dev|build|test)"
---

<objective>
Update all documentation and agent configuration files to reference pnpm instead of bun.

Purpose: Ensure developer docs, AI instructions, and agent configs match the actual toolchain.
Output: Documentation and agent configs that consistently reference pnpm.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-migrate-to-pnpm-turbo/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all documentation and agent configuration files</name>
  <files>CLAUDE.md, DEVELOPER.md, apps/blog/CLAUDE.md, apps/web/TESTING.md, .docs/AGENT_SETUP.md, .docs/QUICK_START.md, apps/web/scripts/dev-apps.sh, .claude/agents/admin-panel.md, .claude/agents/frontend-perf.md, .claude/agents/nextjs-frontend.md</files>
  <action>
1. Read all files fully first.
2. In `CLAUDE.md` (lines ~54-56): Replace `bun dev`, `bun build`, `bun preview` commands with `pnpm dev`, `pnpm build`, `pnpm preview`. Search entire file for any other bun references.
3. In `DEVELOPER.md`: Replace ALL bun references (lines ~44, ~60, ~284-286, ~382, ~453, ~461, ~486 per research). Specifically:
   - Replace `bun install` with `pnpm install`
   - Replace `bun dev` with `pnpm dev`
   - Replace `bun run` with `pnpm run`
   - Replace `bun add` with `pnpm add`
   - Replace `bunx` with `pnpm dlx` or `npx`
   - Update any "Prerequisites" sections to mention pnpm instead of bun
   - Keep the same structure and formatting, just swap the tool references
4. In `apps/blog/CLAUDE.md`: Replace all 7 bun references including `"Package manager": bun` with pnpm equivalents. Replace `bun dev`, `bun build`, `bun preview`, `bun install`, `bun add` etc.
5. In `apps/web/TESTING.md`: Replace all 17 bun references. Replace `bun test`, `bun run test`, `bun dev`, and any other bun commands with pnpm equivalents.
6. In `.docs/AGENT_SETUP.md`: Replace all bun references with pnpm equivalents.
7. In `.docs/QUICK_START.md`: Replace all bun references with pnpm equivalents.
8. In `apps/web/scripts/dev-apps.sh`: Replace all bun references with pnpm equivalents.
9. In `.claude/agents/admin-panel.md` (line ~27): Replace bun reference with pnpm equivalent.
10. In `.claude/agents/frontend-perf.md` (lines ~20, ~108): Replace bun references with pnpm equivalents.
11. In `.claude/agents/nextjs-frontend.md` (line ~23): Replace bun reference with pnpm equivalent.
12. Run grep to confirm zero remaining bun references across all files.
  </action>
  <verify>
    <automated>grep -n "\bbun\b" CLAUDE.md DEVELOPER.md apps/blog/CLAUDE.md apps/web/TESTING.md .docs/AGENT_SETUP.md .docs/QUICK_START.md apps/web/scripts/dev-apps.sh .claude/agents/admin-panel.md .claude/agents/frontend-perf.md .claude/agents/nextjs-frontend.md 2>&1 | grep -v ":0$" && echo "FAIL: bun references remain" || echo "PASS"</automated>
  </verify>
  <done>All documentation and agent configuration files reference only pnpm. No bun mentions remain in any covered file.</done>
</task>

</tasks>

<verification>
```bash
grep -rn "\bbun\b" CLAUDE.md DEVELOPER.md apps/blog/CLAUDE.md apps/web/TESTING.md .docs/AGENT_SETUP.md .docs/QUICK_START.md apps/web/scripts/dev-apps.sh .claude/agents/admin-panel.md .claude/agents/frontend-perf.md .claude/agents/nextjs-frontend.md
# Should return empty
```
</verification>

<success_criteria>
- All documentation files use pnpm commands exclusively
- All agent configuration files use pnpm commands exclusively
- Zero bun references in any documentation, script, or agent config file covered by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-to-pnpm-turbo/12-02-SUMMARY.md`
</output>

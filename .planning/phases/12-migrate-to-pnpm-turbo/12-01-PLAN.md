---
phase: 12-migrate-to-pnpm-turbo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bun.lock
  - bunfig.toml
  - deploy/podman-compose.yml
  - deploy/podman-compose.override.yml
  - Makefile
  - scripts/dev-apps.sh
  - apps/web/package.json
autonomous: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - "No file in the repository references bun as a package manager or runtime"
    - "Container images use node:20-alpine instead of oven/bun:1-alpine"
    - "All dev scripts use pnpm commands"
  artifacts:
    - path: "deploy/podman-compose.yml"
      provides: "Container definitions with node:20-alpine"
      contains: "node:20-alpine"
    - path: "Makefile"
      provides: "Dev and test targets using pnpm"
      contains: "pnpm"
    - path: "scripts/dev-apps.sh"
      provides: "Dev script using pnpm"
      contains: "pnpm dev"
  key_links:
    - from: "Makefile"
      to: "pnpm"
      via: "dev and test targets"
      pattern: "pnpm (dev|run test)"
    - from: "deploy/podman-compose.yml"
      to: "node:20-alpine"
      via: "image field"
      pattern: "image: node:20-alpine"
---

<objective>
Remove all bun artifacts and replace bun references with pnpm equivalents across scripts, containers, and config files.

Purpose: Eliminate dual package manager state left over from Phase 5 migration.
Output: Clean codebase with zero bun references in operational files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-migrate-to-pnpm-turbo/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete bun artifacts and update container images</name>
  <files>bun.lock, bunfig.toml, deploy/podman-compose.yml, deploy/podman-compose.override.yml</files>
  <action>
1. Delete `bun.lock` and `bunfig.toml` from repo root.
2. In `deploy/podman-compose.yml`, replace all 3 occurrences of `image: oven/bun:1-alpine` with `image: node:20-alpine` (lines ~55, ~67, ~77 for hr, hr-blog, hr-docs services).
3. In `deploy/podman-compose.override.yml`:
   - Line ~36: Replace `bun install && bun run build && bunx serve` with `npm i -g pnpm && pnpm install && pnpm build && npx serve` (keep the same port/path arguments).
   - Line ~44: Replace `bun install && cd apps/docs && bun dev` with `npm i -g pnpm && pnpm install && cd apps/docs && pnpm dev`.
   - Check for any other bun references in this file and replace similarly.
  </action>
  <verify>
    <automated>grep -r "bun" bun.lock bunfig.toml deploy/podman-compose.yml deploy/podman-compose.override.yml 2>&1 | grep -v "Binary" | grep -c "bun" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL: bun references remain"</automated>
  </verify>
  <done>bun.lock and bunfig.toml deleted. All container images use node:20-alpine. All container commands use pnpm/npx.</done>
</task>

<task type="auto">
  <name>Task 2: Update Makefile, dev scripts, and package.json</name>
  <files>Makefile, scripts/dev-apps.sh, apps/web/package.json</files>
  <action>
1. In `Makefile`:
   - Lines ~82, ~85, ~88: Replace `bun dev` with `pnpm dev` in dev targets.
   - Lines ~121, ~124, ~127: Replace `bun run test` with `pnpm run test` in test targets.
   - Search for any other bun references and replace.
2. In `scripts/dev-apps.sh`:
   - Lines ~49, ~52, ~55: Replace `bun dev` with `pnpm dev` for all three apps.
   - Search for any other bun references and replace.
3. In `apps/web/package.json`:
   - Line ~19: Replace `"test:all": "bun run test"` with `"test:all": "pnpm run test"`.
4. Run `grep -rn "bun" Makefile scripts/dev-apps.sh apps/web/package.json` to confirm zero remaining references.
  </action>
  <verify>
    <automated>grep -rn "\bbun\b" Makefile scripts/dev-apps.sh apps/web/package.json 2>&1 | grep -c "bun" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL: bun references remain"</automated>
  </verify>
  <done>All Makefile targets use pnpm. dev-apps.sh uses pnpm. package.json script uses pnpm.</done>
</task>

</tasks>

<verification>
Run from repo root:
```bash
# Confirm no bun references in any operational file (excluding docs, git history, node_modules)
grep -rn --include="*.yml" --include="*.yaml" --include="*.sh" --include="*.json" --include="Makefile" "\bbun\b" . | grep -v node_modules | grep -v .git | grep -v RESEARCH
# Should return empty
```
</verification>

<success_criteria>
- bun.lock and bunfig.toml no longer exist
- Zero bun references in deploy/, Makefile, scripts/, and apps/web/package.json
- Container images all use node:20-alpine
</success_criteria>

<output>
After completion, create `.planning/phases/12-migrate-to-pnpm-turbo/12-01-SUMMARY.md`
</output>

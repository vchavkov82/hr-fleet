---
phase: 05-foundation-and-monorepo-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - turbo.json
  - package.json
  - apps/web/package.json
  - apps/blog/package.json
  - apps/docs/package.json
  - services/api/package.json
  - services/worker/package.json
  - apps/admin/package.json
  - .gitignore
autonomous: true
requirements: [FND-01]

must_haves:
  truths:
    - "pnpm install succeeds with new workspace layout"
    - "turbo run build runs across all workspace members without errors"
    - "Existing marketing site (apps/web) builds and serves correctly after relocation"
    - "Go API service builds from services/api/"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Relocated marketing site"
    - path: "apps/blog/package.json"
      provides: "Relocated blog"
    - path: "apps/docs/package.json"
      provides: "Relocated docs"
    - path: "services/api/package.json"
      provides: "Go API as Turborepo workspace member"
    - path: "services/worker/package.json"
      provides: "Go worker scaffold as Turborepo workspace member"
    - path: "apps/admin/package.json"
      provides: "Empty admin scaffold"
    - path: "pnpm-workspace.yaml"
      provides: "Updated workspace config"
    - path: "turbo.json"
      provides: "Updated task graph with Go outputs"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "apps/*, services/*, packages/*"
      via: "workspace glob patterns"
      pattern: "apps/\\*|services/\\*|packages/\\*"
    - from: "turbo.json"
      to: "services/api/package.json"
      via: "turbo task execution"
      pattern: "bin/\\*\\*"
---

<objective>
Restructure the repository into a standard monorepo layout with pnpm workspaces + Turborepo.

Purpose: All subsequent phases depend on the monorepo structure. This relocates existing apps (www/ -> apps/web/, blog/ -> apps/blog/, docs/ -> apps/docs/, backend/ -> services/api/), creates new scaffolds (services/worker/, apps/admin/), and updates workspace and Turborepo configs.

Output: Working monorepo where `pnpm install` and `turbo run build` succeed across all workspace members.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-and-monorepo-setup/05-RESEARCH.md
@pnpm-workspace.yaml
@turbo.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate existing apps and backend into monorepo layout</name>
  <files>apps/web/, apps/blog/, apps/docs/, services/api/, pnpm-workspace.yaml, .gitignore</files>
  <action>
1. Create directory structure: `mkdir -p apps services packages`

2. Move existing directories using `git mv`:
   - `git mv www apps/web`
   - `git mv blog apps/blog`
   - `git mv docs apps/docs`
   - `git mv backend services/api`

3. Update `pnpm-workspace.yaml` to:
```yaml
packages:
  - "apps/*"
  - "services/*"
  - "packages/*"
```

4. Update Go module path in `services/api/go.mod`:
   - Change `module github.com/vchavkov/hr-backend` to `module github.com/vchavkov/hr/services/api`
   - Find-replace all Go import paths: `github.com/vchavkov/hr-backend` -> `github.com/vchavkov/hr/services/api` across all .go files in services/api/

5. Create thin `services/api/package.json` for Turborepo:
```json
{
  "name": "@hr/api",
  "private": true,
  "scripts": {
    "build": "go build -o bin/server ./cmd/server",
    "test": "go test ./...",
    "lint": "golangci-lint run ./...",
    "dev": "go run ./cmd/server"
  }
}
```

6. Update `packages/design-system/tailwind.config.ts` — fix content paths from `../../www/` to `../../apps/web/` (see Research pitfall 1).

7. Update `.gitignore` to include `services/*/bin/` for Go build outputs.

8. Update any references to old paths in root `Makefile`, `package.json` scripts, or `deploy/podman-compose.yml`.

9. Run `pnpm install` to validate workspace resolution.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr && pnpm install --frozen-lockfile 2>&1 || pnpm install 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>
- All 4 directories relocated via git mv (www->apps/web, blog->apps/blog, docs->apps/docs, backend->services/api)
- Go import paths updated in all .go files (no references to old module path)
- pnpm install succeeds with workspace resolution
- .gitignore updated for Go build outputs
- No broken path references in Makefile, package.json, or compose files
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scaffolds and update Turborepo config</name>
  <files>services/worker/package.json, services/worker/go.mod, services/worker/cmd/worker/main.go, apps/admin/package.json, turbo.json</files>
  <action>
1. Create `services/worker/` scaffold:
   - `services/worker/go.mod` with `module github.com/vchavkov/hr/services/worker` and `go 1.25`
   - `services/worker/cmd/worker/main.go` — minimal main with `fmt.Println("worker starting")` and graceful shutdown signal handling
   - `services/worker/package.json`:
```json
{
  "name": "@hr/worker",
  "private": true,
  "scripts": {
    "build": "go build -o bin/worker ./cmd/worker",
    "test": "go test ./...",
    "lint": "golangci-lint run ./...",
    "dev": "go run ./cmd/worker"
  }
}
```

2. Create `apps/admin/` minimal scaffold:
   - `apps/admin/package.json` with `"name": "@hr/admin"`, `"private": true`, placeholder scripts (build: echo "admin not yet implemented", dev: echo "admin not yet implemented")
   - `apps/admin/README.md` — one line: "Admin UI — implemented in Phase 7"

3. Update `turbo.json` to include Go binary outputs:
   - Add `"bin/**"` to build task outputs array
   - Ensure existing outputs (.next/**, dist/**, .astro/**) are preserved
   - Verify task definitions: build, test, lint, typecheck, dev all present

4. Run `turbo run build --dry-run` to validate the full task graph resolves without errors.
  </action>
  <verify>
    <automated>cd /home/vchavkov/src/hr && pnpm turbo run build --dry-run 2>&1 | tail -20; echo "EXIT: $?"</automated>
  </verify>
  <done>Worker scaffold builds, admin scaffold exists, turbo task graph resolves all workspace members</done>
</task>

</tasks>

<verification>
- `pnpm install` completes without errors
- `pnpm turbo run build --dry-run` shows all workspace members in task graph
- `cd services/api && go build ./cmd/server` succeeds with new module path
- `cd services/worker && go build ./cmd/worker` succeeds
- `cd apps/web && pnpm dev` starts the marketing site (manual spot check)
</verification>

<success_criteria>
- Monorepo layout: apps/web, apps/blog, apps/docs, apps/admin, services/api, services/worker, packages/design-system
- All workspace members discoverable by pnpm and Turborepo
- Go services build with correct module paths
- No broken imports in relocated projects
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-and-monorepo-setup/05-01-SUMMARY.md`
</output>

# Browser + server: Go API origin only (no /api/v1 suffix — app paths add it).
# Direct API (Podman / make dev-backend): published on host 5080
NEXT_PUBLIC_API_URL=http://localhost:5080
# If you use an external Caddy gateway on :5090 (see deploy/podman-compose.override.yml comment):
# NEXT_PUBLIC_API_URL=http://hr.localhost:5090
